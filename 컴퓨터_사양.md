# MAKECOMPUTER/8 컴퓨터 사양

이 문서는 `MAKECOMPUTER/8` 브레드보드 컴퓨터의 전체 사양을 한눈에 확인하기 위한 요약 문서다.

> 목표: 외부 MCU가 CPU 역할을 대신하지 않고, 74HC 계열 논리 IC와 RAM/ROM을 직접 연결해 동작하는 8비트 컴퓨터를 만든다.

---

## 1. 기본 사양

| 항목 | 사양 |
|---|---|
| 컴퓨터 종류 | 8비트 브레드보드 컴퓨터 |
| 데이터 폭 | 8비트 |
| DATA BUS | DB0~DB7, 8비트 공용 버스 |
| 주소 폭 | 8비트, A0~A7 |
| 논리 주소 범위 | 0~255 |
| Program Counter | 8비트, 74HC161 ×2 |
| Microstep Counter | 74HC161 ×1 |
| 명령어 크기 | 8비트 |
| 실행 단계 | T0~T5, 최대 6 Microstep |
| 연산 | 8비트 덧셈 / 뺄셈 |
| 전원 | +5V 공통 |
| 기본 Clock | NE555 기반 가변 Clock |
| 구현 방식 | 브레드보드 + 74HC 계열 논리 IC |
| 외부 MCU | CPU 실행에는 사용하지 않음 |

---

## 2. CPU 데이터 흐름

```text
                 +------------------+
                 |  8-bit PC        |
                 | 74HC161 ×2       |
                 +--------+---------+
                          |
                          v
                 +------------------+
                 | Address MUX      |
                 | 74HC157 ×2       |
                 +--------+---------+
                          |
                       A0~A7
                          |
            +-------------+-------------+
            |                           |
            v                           v
      +-----------+               Memory-mapped I/O
      | RAM       |
      | 160 Byte  |
      +-----+-----+
            |
          RAM_D0~7
            |
     +------+-------+
     | RAM-side 245 |
     +------+-------+
            |
        DB0~DB7
            |
    +-------+-------------------------------+
    |            |            |            |
    v            v            v            v
 A Register      IR          OUT       기타 버스 장치
 74HC273      74HC273      74HC273
    |
    v
  8-bit ALU
74HC86 ×2
74HC283 ×2
    |
    v
A/ALU MUX
74HC157 ×2
    |
    v
74HC245
    |
    +--------------> DATA BUS
```

---

## 3. 레지스터

### PC — Program Counter

- 8비트
- 74HC161 두 개를 직렬로 연결
- `PC0~PC7` 사용
- 다음 명령어 주소를 가리킴
- 일반 실행에서 PC 증가 가능
- JMP 등에서 새 주소 Load 가능

### MAR — Memory Address Register

- 8비트 74HC273
- 명령어 다음 바이트에서 읽은 메모리 주소를 저장
- Address MUX를 통해 PC 주소와 MAR 주소 중 하나를 선택

### A Register

- 8비트 74HC273
- ALU의 A 입력값 저장
- DATA BUS에서 값을 받아 저장

### IR — Instruction Register

- 8비트 74HC273
- 현재 실행 중인 명령어 저장
- IR 비트가 명령 디코더로 전달됨

### OUT Register

- 8비트 74HC273
- CPU 출력값을 저장
- 이후 표시 장치/LCD 출력에 사용

---

## 4. ALU

### 구성

```text
A Register --------------------+
                               |
RAM_D0~7 -> 74HC86 ×2 -> 74HC283 ×2 -> 8-bit Result
                               |
                         A/ALU MUX
```

- 74HC283 ×2: 4비트 가산기 두 개를 연결해 8비트 연산
- 74HC86 ×2: 뺄셈 시 RAM 데이터 B를 반전
- 하위 74HC283의 Carry Out을 상위 74HC283의 Carry In에 연결

### 연산 모드

```text
SUB_MODE = 0
A + B

SUB_MODE = 1
A + NOT(B) + 1
= A - B
```

### ALU 출력 선택

74HC157 두 개로 다음 중 하나를 선택한다.

```text
ALU_SELECT = 0 -> A Register 값
ALU_SELECT = 1 -> ALU 결과
```

선택된 8비트 값은 74HC245를 통해 DATA BUS로 출력한다.

---

## 5. 메모리

### RAM

- CDP1824CE ×5
- 각 RAM은 32 × 8bit
- 총 RAM 용량: **160 Byte**

| RAM | 주소 범위 |
|---|---:|
| RAM #1 | 0~31 |
| RAM #2 | 32~63 |
| RAM #3 | 64~95 |
| RAM #4 | 96~127 |
| RAM #5 | 128~159 |

상위 주소 비트 `A5~A7`을 74HC138로 디코딩해 RAM Bank를 선택한다.

### 주소 공간

8비트 주소를 사용하므로 논리적으로 총 256개의 주소를 표현할 수 있다.

현재 배정:

| 주소 | 용도 |
|---:|---|
| 0~159 | RAM |
| 160 | KEY_STATUS |
| 161 | KEY_DATA |
| 162 | LCD_DATA |
| 163 | LCD_COMMAND |
| 164~255 | 현재 미할당/확장 가능 영역 |

I/O도 주소를 통해 접근하는 **Memory-mapped I/O** 구조다.

---

## 6. 명령어 집합

현재 설계된 명령어는 총 13개다.

### 기본 명령

| Opcode 예시 | 명령 | 역할 |
|---|---|---|
| `00100000` | LDA | 메모리 값을 A Register에 Load |
| `01000000` | STA | A Register 값을 메모리에 Store |
| `01100000` | ADD | A + Memory |
| `10000000` | SUB | A - Memory |
| `10100000` | OUT | 값을 OUT Register로 출력 |
| `11000000` | JMP | 지정 주소로 이동 |
| `11100000` | HLT | CPU 정지 |

### 확장 명령

| Opcode | 명령 | 역할 |
|---|---|---|
| `00000000` | NOP | 아무 작업 없이 다음 명령으로 진행 |
| `00000001` | LDI | 즉시값 Load |
| `00000010` | CMP | 비교 |
| `00000011` | JZ | Zero일 때 Jump |
| `00000100` | JNZ | Zero가 아닐 때 Jump |
| `00000101` | RUN | Monitor/Boot 상태에서 실행 상태로 전환 |

---

## 7. 명령 디코딩

명령어는 IR에 저장한 뒤 74HC139들을 이용해 디코딩한다.

```text
IR
 |
 +-> 기본 명령 디코더
 |      LDA / STA / ADD / SUB / OUT / JMP / HLT
 |
 +-> 확장명령 유효 판정
        |
        +-> 확장 명령 디코더
             NOP / LDI / CMP / JZ / JNZ / RUN
```

74HC138/74HC139의 선택 출력은 Active-Low 방식이므로 `_N`이 붙은 제어 신호는 LOW일 때 활성화된다.

---

## 8. Microstep 제어

각 명령어를 한 Clock에 모두 실행하지 않고 작은 단계로 나눠 실행한다.

```text
T0 -> T1 -> T2 -> T3 -> T4 -> T5 -> T0
```

구성:

- 74HC161: 현재 Microstep 번호 저장
- 74HC138: 카운터 값을 T0~T5 신호로 변환
- T5가 끝나면 74HC161을 `0000`으로 Load해 T0로 복귀

기본적인 명령어 Fetch 흐름:

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장 + PC 증가
T2 : 다음 바이트 주소 준비
T3 : 다음 바이트를 MAR에 저장 + PC 증가
T4 : 메모리 주소를 MAR로 전환
T5 : 명령어별 실제 동작 수행
```

---

## 9. Flag

현재 설계에는 **Zero Flag(Z)** 가 포함된다.

- ALU 결과가 `00000000`인지 판정
- CMP / JZ / JNZ 등의 조건 분기에 사용
- Flag 값은 순차 논리 회로에 저장

---

## 10. Boot / Monitor

Boot/Monitor용 ROM을 포함하는 구조를 설계한다.

- AT28C64B 사용
- PC 주소를 이용해 ROM 데이터 선택
- 별도 74HC245를 통해 DATA BUS와 연결
- Monitor Mode와 일반 실행 모드를 제어
- `RUN` 명령으로 일반 프로그램 실행 상태로 전환

CPU 자체의 명령 실행을 Arduino 같은 외부 MCU가 대신하지 않는다.

---

## 11. 키보드 입력

입력 장치는 **PS/2 키보드**를 사용한다.

예정된 입력 경로:

```text
PS/2 Keyboard
     |
PS/2 Clock / Data
     |
74HC14 등 입력 정형
     |
Bit Counter / Shift Register
     |
Scan Code
     |
AT28C64B Scan Code -> ASCII ROM
     |
KEY_DATA / KEY_STATUS
     |
Memory-mapped I/O
```

주소:

```text
160 = KEY_STATUS
161 = KEY_DATA
```

---

## 12. LCD 출력

20×4 Character LCD를 메모리 맵 방식으로 제어한다.

```text
162 = LCD_DATA
163 = LCD_COMMAND
```

CPU는 일반 메모리에 접근하는 것과 비슷한 방식으로 LCD 데이터와 명령을 출력한다.

---

## 13. 주요 IC 구성

| IC | 주요 역할 |
|---|---|
| NE555 | Clock 생성 |
| 74HC161 | PC / Microstep / PS/2 Counter |
| CDP1824CE | RAM |
| 74HC273 | MAR / A / IR / OUT 등 8비트 Register |
| 74HC283 | 8비트 ALU 가산부 |
| 74HC86 | 뺄셈용 XOR |
| 74HC245 | DATA BUS 연결 및 방향/출력 제어 |
| 74HC157 | Address 선택, A/ALU 선택 |
| 74HC138 | RAM Bank / Microstep / I/O Decode |
| 74HC139 | Instruction Decode |
| 74HC08 | AND 제어 논리 |
| 74HC32 | OR 제어 논리 |
| 74HC04 | NOT 제어 논리 |
| 74HC14 | PS/2 입력 정형 등 |
| AT28C64B | Boot/Monitor ROM, Scan Code 변환 ROM |

---

## 14. 전기적 기준

- 모든 로직은 +5V 기준으로 사용한다.
- 각 IC의 VCC/GND는 공통 전원에 연결한다.
- 각 IC에는 0.1µF 디커플링 커패시터 사용을 권장한다.
- Active-Low 핀은 이름 앞의 `/` 또는 뒤의 `_N`으로 표시한다.
- DATA BUS에는 동시에 여러 출력 장치를 활성화하지 않는다.
- 하나의 출력은 여러 입력으로 분기할 수 있지만, 서로 다른 출력 두 개를 직접 연결하지 않는다.
- **점퍼선 색은 기능을 결정하지 않는다. 어떤 색을 사용해도 전기적으로 올바른 핀끼리 연결되어 있으면 된다.**

---

## 15. 핵심 요약

```text
MAKECOMPUTER/8

8-bit Data
8-bit Address
256 logical addresses
160 Byte RAM
Memory-mapped I/O
8-bit ADD/SUB ALU
13 Instructions
T0~T5 Microstep Control
Zero Flag
Boot/Monitor ROM
PS/2 Keyboard
20×4 LCD
+5V Logic
Breadboard Implementation
No external MCU required for CPU execution
```

세부 핀 배선은 [`단계별 만들기`](./단계별%20만들기/)와 [`컴퓨터만들기.md`](./컴퓨터만들기.md)를 참고한다.
