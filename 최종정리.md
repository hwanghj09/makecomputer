# MAKECOMPUTER/8 최종 정리

> 이 문서는 현재까지 확정한 MAKECOMPUTER/8의 **최종 설계 요약 문서**다.
>
> 세부 구조는 `최종구조.md`, 기존 CPU 설명은 `기본설명.md`, 명령어의 기존 동작은 `명령어.md`, 보유 부품은 `부품목록.md`를 참고한다.
>
> 이 문서의 목적은 앞으로 실제 배선표와 핀 단위 netlist를 작성할 때 **설계 기준이 흔들리지 않도록 최종 구조를 한곳에 고정**하는 것이다.

---

# 1. 최종 목표

Arduino나 Raspberry Pi가 CPU 역할을 대신하지 않는 독립형 8비트 브레드보드 컴퓨터를 만든다.

최종 사용 흐름은 다음과 같다.

```text
POWER ON
   ↓
RESET
   ↓
Monitor ROM 자동 실행
   ↓
20×4 LCD 초기화
   ↓
PS/2 Keyboard 사용 가능
   ↓
MAKECOMPUTER/8
SYSTEM READY
> _
   ↓
RAM 확인 / RAM 수정 / 사용자 프로그램 실행
```

최종적으로 다음 기능을 갖는다.

- 직접 만든 8비트 CPU
- RAM 160 byte
- 자동 부팅 Monitor ROM
- PS/2 Keyboard
- 20×4 HD44780 문자 LCD
- Memory-Mapped I/O
- Zero Flag
- 조건 분기
- 간단한 Monitor Terminal

---

# 2. 기본 CPU에서 유지하는 구조

기존 CPU의 기본 데이터 경로는 유지한다.

```text
NE555
→ CPU Clock

74HC161 ×2
→ 8bit Program Counter

74HC161 ×1
→ Microstep Counter

74HC273
→ A Register
→ IR
→ MAR
→ OUT Register

74HC283 ×2 + 74HC86 ×2
→ 8bit ADD / SUB ALU

74HC157 ×2
→ PC / MAR Address 선택

74HC157 ×2
→ A / ALU 결과 선택

74HC245
→ RAM ↔ DATA BUS

74HC245
→ A / ALU → DATA BUS

74HC245
→ Manual Input → DATA BUS

74HC138
→ Microstep Decoder

74HC138
→ RAM Bank Decoder

74HC139
→ 기본 Opcode Decoder
```

---

# 3. 최종 메모리 맵

## RAM

```text
0   ~  31 : RAM #1
32  ~  63 : RAM #2
64  ~  95 : RAM #3
96  ~ 127 : RAM #4
128 ~ 159 : RAM #5
```

총 RAM:

```text
160 byte
```

## Memory-Mapped I/O

```text
160 : KEY_STATUS
161 : KEY_DATA
162 : LCD_DATA
163 : LCD_COMMAND

164 ~ 191 : 예약
192 ~ 255 : 예약
```

기존에 검토했던 `BOOT_CONTROL = 164` 방식은 사용하지 않는다.

Monitor에서 RAM 프로그램으로 넘어갈 때는 `RUN addr` 명령이 PC 변경과 MONITOR_MODE 해제를 동시에 수행한다.

---

# 4. 최종 명령어 체계

기존 3bit Opcode 체계는 유지한다.

```text
IR7 IR6 IR5 | IR4 IR3 IR2 IR1 IR0
------------+---------------------
 기본 Opcode | 확장 영역
```

## 기존 명령

| 코드 | 명령 | 길이 |
|---|---|---:|
| `00100000` | `LDA addr` | 2 byte |
| `01000000` | `STA addr` | 2 byte |
| `01100000` | `ADD addr` | 2 byte |
| `10000000` | `SUB addr` | 2 byte |
| `10100000` | `OUT` | 1 byte |
| `11000000` | `JMP addr` | 2 byte |
| `11100000` | `HLT` | 1 byte |

## 확장 명령

`000xxxxx`를 확장 명령 그룹으로 사용한다.

| 코드 | 명령 | 길이 | 의미 |
|---|---|---:|---|
| `00000000` | `NOP` | 1 | 아무 동작 없음 |
| `00000001` | `LDI value` | 2 | 다음 바이트를 A에 직접 저장 |
| `00000010` | `CMP addr` | 2 | A와 메모리 값을 비교, A는 유지 |
| `00000011` | `JZ addr` | 2 | Z=1이면 Jump |
| `00000100` | `JNZ addr` | 2 | Z=0이면 Jump |
| `00000101` | `RUN addr` | 2 | PC←addr + MONITOR_MODE 해제 |
| `00000110` | Reserved | - | 향후 확장 |
| `00000111` | Reserved | - | 향후 확장 |

V1에서는 `IR4=0`, `IR3=0`을 확장 명령 유효 조건으로 사용하고 `IR2~IR0`으로 세부 명령을 구분한다.

---

# 5. 최종 Microstep 정책

모든 명령을 최대 `T0~T5` 범위 안에서 처리한다.

공통 Fetch:

```text
T0 : PC 주소 경로 선택 / 안정화
T1 : [PC] → IR, PC + 1
```

2바이트 명령은 다음과 같이 Operand를 읽는다.

```text
T2 : PC 주소 경로 준비
T3 : Operand 처리, PC + 1
T4 : MAR 또는 실행 경로 안정화
T5 : 실제 명령 실행
```

## 명령별 T0~T5 요약

| 명령 | T0 | T1 | T2 | T3 | T4 | T5 |
|---|---|---|---|---|---|---|
| NOP | PC | IR←[PC], PC++ | - | - | - | 종료 |
| LDI | PC | IR←[PC], PC++ | PC | A←[PC], PC++ | - | 종료 |
| LDA | PC | IR←[PC], PC++ | PC | MAR←[PC], PC++ | MAR | A←[MAR] |
| STA | PC | IR←[PC], PC++ | PC | MAR←[PC], PC++ | MAR | [MAR]←A |
| ADD | PC | IR←[PC], PC++ | PC | MAR←[PC], PC++ | MAR | A←A+[MAR], Z 갱신 |
| SUB | PC | IR←[PC], PC++ | PC | MAR←[PC], PC++ | MAR | A←A-[MAR], Z 갱신 |
| CMP | PC | IR←[PC], PC++ | PC | MAR←[PC], PC++ | MAR | A-[MAR], A 유지, Z만 갱신 |
| OUT | PC | IR←[PC], PC++ | - | - | - | OUT←A |
| JMP | PC | IR←[PC], PC++ | PC | MAR←[PC], PC++ | - | PC←MAR |
| JZ | PC | IR←[PC], PC++ | PC | MAR←[PC], PC++ | - | Z=1이면 PC←MAR |
| JNZ | PC | IR←[PC], PC++ | PC | MAR←[PC], PC++ | - | Z=0이면 PC←MAR |
| RUN | PC | IR←[PC], PC++ | PC | MAR←[PC], PC++ | - | PC←MAR, MONITOR_MODE←0 |
| HLT | PC | IR←[PC], PC++ | - | - | - | HALT 상태 진입 |

> 실제 레지스터 clock pulse는 74HC273에 Load Enable이 없기 때문에 **제어 Enable과 CPU Clock을 조합하여 필요한 순간에만 발생**시키는 방식으로 구현한다.

---

# 6. 핵심 제어신호 논리식

표기 규칙:

```text
·  = AND
+  = OR
_N = Active-Low 또는 반전 신호
```

## 2바이트 명령 검출

```text
BYTE2_A  = NOP_N · OUT_N
BYTE2    = BYTE2_A · HLT_N
T3_BYTE2 = T3 · BYTE2
```

## PC Count

```text
PC_COUNT = T1 + T3_BYTE2
```

즉:

- 모든 명령 Fetch 후 1회 증가
- 2바이트 명령의 Operand를 읽은 뒤 1회 추가 증가

## MAR

```text
MAR_EN  = T3_BYTE2 · LDI_N
MAR_CLK = CPU_CLK · MAR_EN
```

LDI는 T3에서 A에 직접 값을 넣으므로 MAR를 갱신하지 않는다.

## A Register

```text
LDI_T3 = T3 · LDI
A_EXEC = LDA + ADD + SUB
A_EN   = LDI_T3 + T5·A_EXEC
A_CLK  = CPU_CLK · A_EN
```

CMP는 A를 변경하지 않는다.

## IR

```text
IR_CLK = CPU_CLK · T1
```

## SUB Mode

```text
SUB_MODE = SUB + CMP
```

CMP 역시 내부적으로는 `A - MEMORY`를 계산한다.

## Zero Flag

```text
Z_EN  = T5 · (ADD + SUB + CMP)
Z_CLK = CPU_CLK · Z_EN
```

Zero Detector:

```text
ZERO = NOT(
    ALU7 OR ALU6 OR ALU5 OR ALU4 OR
    ALU3 OR ALU2 OR ALU1 OR ALU0
)
```

## 조건 Jump

```text
JZ_TAKE  = JZ · Z
JNZ_TAKE = JNZ · Z_N

JUMP_ANY = JMP + RUN + JZ_TAKE + JNZ_TAKE

PC_LOAD   = T5 · JUMP_ANY
PC_LOAD_N = NOT(PC_LOAD)
```

RUN의 경우 같은 T5에서:

```text
PC ← MAR
MONITOR_MODE ← 0
```

를 동시에 수행한다.

---

# 7. Zero Flag

Zero Flag는 74HC74의 Flip-Flop 한 개를 사용한다.

```text
ALU RESULT
   ↓
8bit OR Tree
   ↓
NOT
   ↓
ZERO
   ↓
74HC74
   ↓
Z FLAG
```

갱신되는 명령:

```text
ADD
SUB
CMP
```

CMP는 다음과 같다.

```text
A - MEMORY
   ↓
Zero Detector
   ↓
Z Flag 갱신

A Register는 그대로 유지
```

---

# 8. Monitor Mode와 Boot ROM

단순히 주소 0~159를 통째로 ROM으로 바꾸는 Overlay는 사용하지 않는다.

RESET 직후:

```text
MONITOR_MODE = 1
PC = 0
```

Monitor Mode에서:

```text
주소 출처가 PC
→ Boot ROM에서 읽음

주소 출처가 MAR
→ RAM / I/O에서 읽음
```

이 방식을 **Fetch-only Monitor ROM** 구조로 사용한다.

## ROM Fetch 조건

```text
ROM_FETCH = MONITOR_MODE · PC_COUNT
```

ROM_FETCH가 활성화될 때만 Boot ROM의 74HC245가 DATA BUS를 구동한다.

```text
Boot ROM
   ↓
74HC245
   ↓
DATA BUS
```

그 외에는 Boot ROM BUS Driver를 High-Z로 둔다.

## Boot ROM 주소

V1에서는 CPU PC가 8비트이므로 Boot ROM 첫 256 byte만 직접 실행한다.

```text
EEPROM A0~A7  ← PC0~PC7
EEPROM A8~A12 → GND
```

즉 Monitor Firmware V1의 실행 공간은:

```text
256 byte
```

이다.

---

# 9. Memory-Mapped I/O Decode

RAM Bank Decoder의 `A7~A5=101` 영역을 I/O용으로 사용한다.

```text
160~191 = 101xxxxx
```

실제 V1 I/O는 160~163만 사용한다.

```text
A4=0
A3=0
A2=0
```

일 때만 I/O Decoder를 활성화한다.

개념식:

```text
IO_G = BANK_Y5_N + A4 + A3 + A2
IO_VALID = NOT(IO_G)
```

그 다음 A1/A0으로 네 주소를 나눈다.

```text
160 : 00 → KEY_STATUS
161 : 01 → KEY_DATA
162 : 10 → LCD_DATA
163 : 11 → LCD_COMMAND
```

## Keyboard Read

```text
KEYSEL   = IO_VALID · NOT(A1)
KEY_READ = T5 · LDA · KEYSEL
KEY_ACK  = KEY_READ · A0
```

`161 KEY_DATA`를 읽으면 KEY_ACK가 발생하여 다음 키 입력을 받을 준비를 한다.

## LCD Write

```text
LCDSEL    = IO_VALID · A1
LCD_WRITE = T5 · STA · LCDSEL
```

LCD 주소 구분:

```text
162 : LCD_DATA
163 : LCD_COMMAND
```

HD44780의 RS 기준:

```text
LCD_RS = NOT(A0)
```

따라서:

```text
162 → A0=0 → LCD_RS=1 → Data
163 → A0=1 → LCD_RS=0 → Command
```

---

# 10. RAM Read / Write 제어

## RAM Read

Monitor가 꺼진 RAM 프로그램 Fetch:

```text
PROG_RAM_READ = MONITOR_N · PC_COUNT
```

데이터 연산용 RAM Read:

```text
READOP = LDA + ADD + SUB + CMP
DATA_RAM_READ = T5 · READOP
```

최종:

```text
RAM_READ_REQ = PROG_RAM_READ + DATA_RAM_READ
```

RAM의 실제 read polarity는 CDP1824CE 데이터시트 기준으로 최종 핀 배선 시 확정한다.

## RAM Write

```text
STA_T5 = STA · T5
RAM_WRITE_PULSE = CPU_CLK · STA_T5
```

STA 대상 주소가 0~159이면 RAM에 쓰고, 160~163이면 해당 Memory-Mapped I/O 장치를 제어한다.

---

# 11. PS/2 Keyboard Interface

최종 목표는 MCU 없이 PS/2 Keyboard를 받는 것이다.

전체 흐름:

```text
PS/2 Keyboard
   │
   ├── DATA
   └── CLOCK
        ↓
      74HC14
        ↓
    74HC164 #1
        ↓
    74HC164 #2
        ↓
  PS/2 Frame Capture
        ↓
  Scan Code → ASCII ROM
        ↓
      74HC245
        ↓
      DATA BUS
```

PS/2 신호선에는 Pull-up 저항을 사용한다.

```text
+5V
 │
4.7kΩ
 │
PS/2 DATA
```

```text
+5V
 │
4.7kΩ
 │
PS/2 CLOCK
```

키 한 프레임이 준비되면 `KEY_READY=1` 상태를 만들고 CPU가 `KEY_DATA`를 읽을 때 `KEY_ACK`로 수신 상태를 초기화한다.

느린 CPU Clock에서도 키 입력을 잃지 않도록 PS/2 Clock Hold용 NPN 트랜지스터 1개를 사용하는 구조를 채택한다.

---

# 12. Keyboard ASCII ROM

AT28C64B #1을 Scan Code → ASCII 변환 ROM으로 사용한다.

```text
PS/2 Scan Code
      ↓
AT28C64B #1
      ↓
ASCII 8bit
```

예:

```text
A Key
→ Scan Code
→ Lookup ROM
→ 01000001
→ ASCII 'A'
```

EEPROM 내부에는 변환표를 미리 기록한다.

---

# 13. LCD Interface

LCD는 다음 규격을 사용한다.

```text
20×4 Character LCD
HD44780 compatible
5V
16pin Parallel
```

I²C Backpack 없이 병렬 연결한다.

V1에서는 LCD Read를 사용하지 않고 Write Only로 구성한다.

```text
LCD R/W → GND
```

CPU 출력은 OUT Register를 LCD Data Latch 용도로도 재사용한다.

```text
OUT_EN  = (OUT · T5) + LCD_WRITE
OUT_CLK = CPU_CLK · OUT_EN
```

LCD Enable Pulse:

```text
LCD_E = CPU_CLK · LCD_WRITE
```

---

# 14. Monitor Program V1

처음부터 긴 Shell을 만드는 대신 256 byte ROM 안에 들어가는 Compact Monitor를 목표로 한다.

초기 명령 예시:

```text
H
→ HELP

M aa
→ RAM aa 읽기

S aa vv
→ RAM aa에 vv 저장

R aa
→ RAM aa부터 프로그램 실행

C
→ LCD Clear
```

화면 예시:

```text
MAKECOMPUTER/8
SYSTEM READY
> M 20
05
> S 20 42
OK
> R 00
```

사용자 프로그램에서 Monitor로 돌아오는 기능은 V1에서는 별도 명령으로 구현하지 않고 RESET 버튼을 사용한다.

```text
RESET
→ PC=0
→ MONITOR_MODE=1
→ Monitor 재실행
```

---

# 15. 제어 게이트 수량 계산

현재 최종 논리식 기준 예상 사용량:

| 부품 | 예상 필요 | 현재 보유 | 추가 필요 |
|---|---:|---:|---:|
| **74HC08** | 약 **11개 / 44 gate** | 5 | **+6개** |
| **74HC32** | 약 **7개 / 28 gate** | 5 | **+2개** |
| **74HC04** | 약 **4개 / 24 inverter** | 4 | 0 |
| **74HC138** | 3 | 3 | 0 |
| **74HC139** | 3 | 3 | 0 |
| **74HC157** | 6 | 6 | 0 |
| **74HC245** | 5 | 5 | 0 |

> 위 게이트 수량은 현재 최종 논리식을 2-input AND/OR/NOT으로 직접 구현하는 기준이다. 실제 핀 단위 netlist 최종화 과정에서 공유 가능한 중간 신호를 합치면 일부 줄어들 가능성이 있다.

---

# 16. 확장부 신규/추가 부품

## 신규 핵심 부품

| 부품 | 수량 | 역할 |
|---|---:|---|
| **74HC74** | 2 | Z Flag / MONITOR_MODE / KEY_READY 등 상태 저장 |
| **74HC164** | 2 | PS/2 Serial Frame 수신 |
| **74HC14** | 1 | PS/2 신호 정리 / Schmitt Trigger |
| **AT28C64B** | 2 | ASCII Lookup ROM + Boot/Monitor ROM |
| **NPN Transistor** | 1 | PS/2 Clock Hold |
| **PS/2 Keyboard** | 1 | 사용자 입력 |
| **Mini-DIN 6pin Female** | 1 | PS/2 Keyboard 연결 |
| **20×4 HD44780 LCD** | 1 | 텍스트 출력 |
| **EEPROM Programmer** | 1 | EEPROM 기록 |
| **4.7kΩ Resistor** | 10개 이상 | PS/2 Pull-up + 예비 |

## 추가 구매가 필요한 기존 계열

```text
74HC08  +6개
74HC32  +2개
```

## Decoupling Capacitor

각 IC 근처에 0.1µF Ceramic Capacitor 1개를 기본 원칙으로 한다.

최종 IC 수가 크게 늘어나므로 현재 60개만으로는 부족할 수 있다.

권장 추가:

```text
0.1µF Ceramic Capacitor +20개 이상
```

---

# 17. 현재 IC 번호 계획

핀 단위 최종 netlist 작성 시 아래 번호 체계를 기준으로 한다.

```text
U1       NE555

U2-U4    74HC161
U5-U9    CDP1824CE
U10-U11  74HC283
U12-U13  74HC86
U14-U17  74HC273
U18-U20  74HC245
U21-U24  74HC157
U25-U26  74HC138
U27      74HC139

U28-U38  74HC08
U39-U45  74HC32
U46-U49  74HC04

U50      74HC138
U51      74HC139
U52-U53  74HC74

U54-U55  74HC164
U56      AT28C64B ASCII ROM
U57-U58  74HC157 Keyboard MUX / Selector
U59      74HC245 Keyboard BUS

U60      AT28C64B Boot ROM
U61      74HC245 Boot ROM BUS

U62      74HC14
U63      74HC139

LCD1     HD44780 20×4 LCD
Q1       NPN PS/2 Clock Hold
```

> U57/U58/U63의 세부 기능은 핀 단위 netlist를 작성하면서 최종적으로 신호 배분을 고정한다.

---

# 18. 실제 제작 전에 반드시 검증할 부분

현재 **구조와 논리 수준은 확정**했지만, 다음 항목은 실제 핀 단위 최종 배선 문서에서 반드시 데이터시트와 교차검증한다.

1. CDP1824CE의 정확한 핀 번호
2. CDP1824CE의 Chip Select / Memory Read / Memory Write Active Level
3. AT28C64B `/CE`, `/OE`, `/WE`의 실제 연결
4. 74HC161의 `/LOAD`, ENP, ENT, RCO 연결
5. 74HC273의 Reset polarity와 Clock edge
6. 74HC245의 DIR / `/OE` 방향
7. 74HC157의 Select / `/E` polarity
8. 74HC138 / 74HC139 output polarity
9. PS/2 Keyboard의 실제 Mini-DIN 핀 배열
10. HD44780 모듈의 실제 16핀 배열

이 확인이 끝난 뒤에만 실제 브레드보드 최종 배선으로 들어간다.

---

# 19. 아직 남은 마지막 설계 문서

다음 단계는 별도의 **`최종배선.md`** 문서를 작성하는 것이다.

그 문서는 다음 형식으로 작성한다.

```text
74HC08 U28

1번 ← U27 5번
2번 ← U25 13번
3번 → U39 1번
4번 ← ...
5번 ← ...
6번 → ...
7번 → GND
8번 ← ...
9번 ← ...
10번 → ...
11번 ← ...
12번 ← ...
13번 → ...
14번 → +5V
```

즉:

- 모든 IC
- 모든 핀
- 상대 부품명
- 상대 IC 번호
- 상대 핀 번호
- +5V / GND
- Pull-up / Pull-down
- 사용하지 않는 입력 처리

까지 전부 작성한다.

이 단계에서는 A→B 연결뿐 아니라 **B→A 역참조도 일치하는지 검산**한 뒤 최종 배선표로 확정한다.

---

# 20. 최종 제작 순서

```text
1. 기본 CPU 데이터 경로 완성
   ↓
2. PC / MAR / RAM / ALU / Register 단위 테스트
   ↓
3. 확장 Opcode Decoder 추가
   ↓
4. Zero Detector + Z Flag 추가
   ↓
5. JZ / JNZ / RUN 테스트
   ↓
6. Fetch-only Boot ROM 추가
   ↓
7. Monitor 자동 부팅 테스트
   ↓
8. Memory-Mapped I/O Decoder 추가
   ↓
9. LCD 연결 및 문자 출력 테스트
   ↓
10. PS/2 Keyboard Receiver 제작
   ↓
11. Scan Code → ASCII ROM 연결
   ↓
12. KEY_STATUS / KEY_DATA 테스트
   ↓
13. Monitor Firmware 작성
   ↓
14. RAM Read / Write / RUN 통합 테스트
   ↓
15. 최종 터미널 완성
```

---

# 최종 형태

```text
┌─────────────────────────────────────────────┐
│              MAKECOMPUTER/8                 │
│                                             │
│  직접 만든 8bit CPU                        │
│  + 160 byte RAM                             │
│  + Zero Flag                                │
│  + LDI / CMP / JZ / JNZ / RUN              │
│  + Boot ROM                                 │
│  + Monitor Firmware                         │
│  + PS/2 Keyboard                            │
│  + 20×4 Text LCD                            │
│  + Memory-Mapped I/O                        │
│                                             │
│          외부 MCU 없이 독립 동작            │
└─────────────────────────────────────────────┘
```

최종 목표는 단순한 CPU 시연기가 아니라,

> **전원을 켜면 스스로 Monitor가 부팅되고, 키보드로 명령을 입력하고, LCD로 결과를 확인하고, RAM을 수정하고, 직접 입력한 프로그램을 실행할 수 있는 실제 8비트 브레드보드 컴퓨터**

를 완성하는 것이다.
