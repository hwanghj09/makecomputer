# IC칩들

NE555(1)
- 클럭

74HC161(3)
- 2개는 8비트 Program Counter
- 1개는 Microstep Counter

CDP1824CE(3)
- RAM

74HC283(2)
- 4비트 가산기 2개를 연결해서 8비트 가산기

74HC86(2)
- 8비트 뺄셈용 XOR

74HC273(3)
- 8비트 레지스터

74HC245(3)
- 8비트 버스 드라이버

74HC157(4)
- 2:1 MUX

74HC138(1)
- 명령어 디코더

74HC139(1)
- 마이크로스텝 / 제어 디코더

74HC08(3)
- AND Gate

74HC32(3)
- OR Gate

74HC04(2)
- NOT Gate



---

.

.

.

.


# 클럭(Clock) NE555

클럭(CLOCK) : 논리상태인 1(HIGH), 0(LOW)을 주기적으로 나타내는 구형파

구형파 : 그냥 0,1로만 왔다갔다 하는 거 그 네모난거 있잖아

펄스(PULSE) : 일시적인 클럭 느낌


클럭는 그냥 컴퓨터의 심장같은거라고 이해하면 쉬움

1(HIGH) 일때 딱 하나의 명령를 실행할 수 있어

예를 들어서

`A에 1을 더해라` 라는 명령이 있으면

일단 명령어들을 쉽게쉽게 쪼개

큰 명령어(`A에 1을 더해라`)을 작은 명령어들(`A 읽기`, `1 더하기`, `A에 저장하기`)로

```
    ----------     ----------      --------------
    | A 읽기  |    | 1 더하기 |     | A에 저장하기 |
----          ----           ------             -----
```


이런식으로 하나 딸각할 때마다 하나의 작은 명령어가 실행된다고 생각하면 돼

우린 이 작은 명령어들을 마이크로 명령어라고 부를거야

그리고 저 클럭의 주기? 속도? 아무튼 그걸 가변저항으로 조절할거야

# PC(program counter) 74HC161
pc : 현재 실행 중인 프로그램 주소 기억하는 장치

한 명령어가 끝나면 다음 주소로 넘어가

예를 들어서
```
| 주소 	| 명령어 	|
|------	|--------	|
| 000  	| LDA 20 	|
| 001  	| LDB 21 	|
| 010  	| add B  	|
| 011  	| out A  	|
```
이런식으로 되어있다고 하자

딱 컴퓨터를 켰을 때 pc의 값이 초기화가 되어서 000이야

그 다음 `LDA 20`이라는 명령어가 끝나면 001로 넘어가는거야

`74HC161`를 3개를 샀어

왜냐하면 위에서 말했던 마이크로 명령어도 개별적으로 주소를 세야돼서야

2개는 일반 명령어 주소, 나머지 1개는 마이크로스텝 카운터로 현재 명령어가 몇 번째 마이크로 동작 단계에 있는지를 세는거야


# 메모리(RAM) CDP1824

이건 메모리야 메모리는 알거니깐 넘어가고

RAM은 휘발성 저장장치이기 때문에 전기가 끊기면 안에 저장 내용도 다 사라져

`CDP1824`는 32*8 저장장치야

진짜 간단하게 설명하자면

32층까지 있는 아파트에 각 층마다 0과 1이 있을 수 있는 공간이 8칸이 있는거야

표로 생각을 하면 행 32개, 열 8개인거지

긍까 전체용량은 `32*8bit=256bit`가 돼

근데 우리는 `CDP1824`를 하나만 쓰는게 아니라 3개를 써

그럼 최종 전체 용량은 `256*3`이니깐 `768bit`야

메모리 주소 관리는

```
주소        사용RAM
---------------------- 
0~31        CDP1824#1
32~63       CDP1824#2
64~95       CDP1824#3
```

이런식으로할거야

예를 들어서 37번째에 있는 메모리를 읽고싶다? 그러면

주소 37은 32~63 범위니깐 `CDP1824#2`가 선택되고

`37-32=5`

이니깐 `CDP1824#2`의 5번째 주소를 읽겠지


우리가 PC 부분에서 2개를 일반 명령어 주소를 센다고 했잖아


근데 여기서 굉장히 중요한 점, 이해하기 어려운 점은

PC에서의 값 중에서 일부는 어떤 RAM을 사용할건지, 일부는 어떤 주소를 셀지 선택하는지 정해야돼

```
PC6 PC5 | PC4 PC3 PC2 PC1 PC0
--------+--------------------- 
RAM 선택 | RAM 내부 주소

00 : RAM1
01 : RAM2
10 : RAM3
11 : 사용하지 않음
```

이렇게 할거야

앞에 PC6, PC5는 어떤 렘을 사용할건지, 뒤에 PC4~PC0까지는 그 렘의 몇번지 주소인지를 알려주는거야


예시를 들어줄게

```
PC6 PC5 | PC4 PC3 PC2 PC1 PC0
--------+--------------------- 
  1   0 |    0  0   1   0   1
```
이렇게 되어있으면

`10`, 즉 RAM3에서의 `00101`라는 주소를 가지고 있는거지


