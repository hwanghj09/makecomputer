# MAKECOMPUTER Breadboard Digitalizer

## 1. 프로젝트 한 줄 정의

현실에서 직접 제작한 브레드보드 회로를 웹 화면에 **부품 위치, 방향, 핀, 점퍼선까지 실제 모습 그대로 옮겨 저장하는 브레드보드 디지털화 프로그램**이다.

---

# 2. 프로젝트 목표

목표는 단순하다.

```text
현실 브레드보드
        ↓
웹에서 동일한 브레드보드 배치
        ↓
실제 위치에 IC와 부품 배치
        ↓
실제로 연결한 점퍼선 그대로 연결
        ↓
프로젝트 파일 저장
```

웹에서는 회로가 정상인지 판단하지 않는다.

사용자가 만든 현실 회로를 **있는 그대로 디지털화하는 것**만 담당한다.

---

# 3. 하지 않는 기능

다음 기능은 구현하지 않는다.

* 회로 시뮬레이션
* CPU 실행
* Clock 실행
* RAM 동작
* ALU 계산
* HIGH / LOW 계산
* 전압 계산
* 전류 계산
* 배선 오류 검사
* GitHub 설계와 비교
* AI 분석
* 자동 수정
* 점퍼선 색 검사
* 잘못된 핀 경고

예를 들어 ADDRESS 선을 초록색으로 연결했더라도 프로그램은 아무 말도 하지 않는다.

```text
현실에서 초록색 선
→ 디지털에서도 초록색 선
```

그것이 이 프로그램의 역할이다.

---

# 4. 핵심 설계 철학

프로그램은 사용자가 만드는 회로를 평가하지 않는다.

```text
정답 회로 X
오답 회로 X
```

단순히

```text
현재 현실에서 이렇게 연결되어 있음
```

이라는 상태만 기록한다.

잘못 연결한 선도 그대로 그릴 수 있어야 한다.

---

# 5. 기본 화면 구조

```text
┌───────────────────────────────────────────────────────────────┐
│ MAKECOMPUTER Breadboard Digitalizer                          │
│                                                               │
│ 새로 만들기  열기  저장  다른 이름으로 저장  실행취소       │
│ 다시실행  확대  축소  화면맞춤                               │
├──────────────┬────────────────────────────────┬───────────────┤
│              │                                │               │
│ 부품 목록    │                                │ 속성          │
│              │                                │               │
│ 검색         │        WORKSPACE               │ 선택 부품     │
│              │                                │ 이름          │
│ 브레드보드   │                                │ 위치          │
│ IC           │                                │ 회전          │
│ 저항         │                                │ 메모          │
│ LED          │                                │               │
│ 스위치       │                                │               │
│ 커패시터     │                                │               │
│ 기타         │                                │               │
│              │                                │               │
├──────────────┴────────────────────────────────┴───────────────┤
│ 상태바: Zoom 100% | 선택 없음 | X: 0000 Y: 0000              │
└───────────────────────────────────────────────────────────────┘
```

---

# 6. 작업 공간

작업 공간은 매우 크게 만든다.

브레드보드 10개 이상을 배치해도 공간이 부족하지 않아야 한다.

가상 작업 공간은 사실상 무한 캔버스처럼 사용한다.

예:

```text
                    [BB1] [BB2] [BB3]


        [BB4] [BB5] [BB6]


                    [BB7] [BB8]


            [LCD]          [PS/2]
```

---

# 7. 화면 이동

작업 공간을 자유롭게 이동할 수 있어야 한다.

지원 방법:

### 마우스 가운데 버튼 드래그

```text
Middle Mouse Drag
→ 화면 이동
```

### 빈 공간 드래그

선택 도구 상태에서 빈 공간을 드래그해 이동할 수도 있다.

### Shift + 마우스 드래그

대체 이동 방법으로 지원한다.

---

# 8. 확대 / 축소

매우 중요하다.

지원:

```text
마우스 휠 + Ctrl
```

또는

```text
Ctrl + +
Ctrl + -
```

버튼:

```text
[ - ]  100%  [ + ]
```

권장 범위:

```text
10%
~
500%
```

또한:

```text
화면에 전체 맞춤
선택한 부품에 맞춤
100%로 복귀
```

기능을 제공한다.

---

# 9. 줌 기준점

확대할 때 화면 중앙 기준이 아니라 **마우스 커서가 있는 위치를 기준으로 확대**한다.

그래야 특정 IC를 보고 있을 때 위치가 밀려나지 않는다.

---

# 10. 실행 취소 / 다시 실행

모든 편집 동작에 Undo / Redo가 적용되어야 한다.

```text
Ctrl + Z
→ 실행 취소

Ctrl + Y
→ 다시 실행
```

또는

```text
Ctrl + Shift + Z
→ 다시 실행
```

Undo 대상:

* 부품 추가
* 부품 삭제
* 부품 이동
* 부품 회전
* 점퍼선 추가
* 점퍼선 삭제
* 점퍼선 위치 변경
* 점퍼선 색 변경
* 브레드보드 이동
* 브레드보드 삭제
* 복사
* 붙여넣기
* 이름 변경
* 메모 변경

---

# 11. 선택 기능

마우스로 부품을 클릭하면 선택된다.

선택된 부품은 외곽선을 표시한다.

여러 개 선택:

```text
Shift + 클릭
```

또는 영역 선택:

```text
빈 공간 드래그
```

예:

```text
┌─────────────────┐
│ 74HC161         │
│ PC LOW          │
└─────────────────┘
```

---

# 12. 다중 선택

여러 개의 부품과 점퍼선을 한 번에 선택할 수 있어야 한다.

다중 선택 후:

* 이동
* 삭제
* 복사
* 붙여넣기

가능.

브레드보드 하나에 꽂혀 있는 여러 부품을 한꺼번에 옮기는 데 유용하다.

---

# 13. 복사 / 붙여넣기

```text
Ctrl + C
Ctrl + V
```

지원.

예를 들어 동일한 74HC161을 여러 개 사용하는 경우 빠르게 복제할 수 있다.

붙여넣기 시 약간 오른쪽 아래 위치에 복제한다.

---

# 14. 삭제

```text
Delete
```

선택한 부품이나 점퍼선을 삭제한다.

삭제 역시 Undo 가능해야 한다.

---

# 15. 회전

```text
R
```

키를 누르면 선택된 부품을 회전한다.

```text
0°
90°
180°
270°
```

지원.

필요하면 우측 속성 창에서도 회전 값을 선택할 수 있다.

---

# 16. 정렬 보조

부품을 이동할 때 브레드보드 구멍이나 일정한 격자에 자동으로 맞춰준다.

```text
Snap to Grid
```

기능.

설정에서 켜기/끄기 가능:

```text
☑ 격자 맞춤
```

Alt 키를 누른 상태에서는 잠시 Snap을 해제할 수 있다.

---

# 17. 브레드보드

실제 긴 브레드보드를 표현한다.

```text
+  -  ● ● ● ● ● ● ● ● ● ● ● ● ●
+  -  ● ● ● ● ● ● ● ● ● ● ● ● ●

A     ● ● ● ● ● ● ● ● ● ● ● ● ●
B     ● ● ● ● ● ● ● ● ● ● ● ● ●
C     ● ● ● ● ● ● ● ● ● ● ● ● ●
D     ● ● ● ● ● ● ● ● ● ● ● ● ●
E     ● ● ● ● ● ● ● ● ● ● ● ● ●

-----------------------------------

F     ● ● ● ● ● ● ● ● ● ● ● ● ●
G     ● ● ● ● ● ● ● ● ● ● ● ● ●
H     ● ● ● ● ● ● ● ● ● ● ● ● ●
I     ● ● ● ● ● ● ● ● ● ● ● ● ●
J     ● ● ● ● ● ● ● ● ● ● ● ● ●

+  -  ● ● ● ● ● ● ● ● ● ● ● ● ●
+  -  ● ● ● ● ● ● ● ● ● ● ● ● ●
```

실제 사용하는 긴 브레드보드 형태를 기본으로 한다.

---

# 18. 브레드보드 여러 개

개수 제한 없이 추가할 수 있게 한다.

```text
+ 브레드보드 추가
```

각 브레드보드 이름 지정 가능.

예:

```text
BB1
PC

BB2
MUX / MAR

BB3
RAM

BB4
ALU
```

---

# 19. 브레드보드 이동

브레드보드 전체를 드래그해서 위치를 변경한다.

선택적으로:

```text
브레드보드를 옮길 때
위에 꽂혀 있는 부품도 같이 이동
```

한다.

점퍼선의 끝점 역시 함께 따라간다.

---

# 20. 부품 라이브러리

왼쪽 패널에 모든 부품을 분류한다.

```text
브레드보드

IC
Memory
Display
Input
Power
Passive
Connector
Other
```

---

# 21. 반드시 포함할 IC

## Clock

* NE555

## Counter

* 74HC161

## Register

* 74HC273

## Bus Transceiver

* 74HC245

## Multiplexer

* 74HC157

## Arithmetic

* 74HC283
* 74HC86

## Decoder

* 74HC138
* 74HC139

## Logic Gate

* 74HC08
* 74HC32
* 74HC04

## Flip-Flop

* 74HC74

## PS/2

* 74HC14
* 74HC164

## Memory

* CDP1824CE
* AT28C64B

---

# 22. IC 검색

검색창 제공:

```text
부품 검색...
```

예:

```text
161
```

검색 시:

```text
74HC161
```

표시.

또는:

```text
mux
```

검색 시:

```text
74HC157
```

표시.

---

# 23. DIP IC 표현

IC는 현실 모습과 비슷하게 표시한다.

예:

```text
            74HC161
       ┌──────────────┐
 /CLR 1│●            │16 VCC
  CLK 2│             │15 RCO
    A 3│             │14 QA
    B 4│             │13 QB
    C 5│             │12 QC
    D 6│             │11 QD
  ENP 7│             │10 ENT
  GND 8│              │9 /LOAD
       └──────────────┘
```

필수 요소:

* 품번
* DIP 모양
* 홈
* 1번 핀 표시
* 핀 번호
* 핀 이름

---

# 24. 핀 번호 표시 설정

회로가 커지면 핀 번호 표시가 복잡할 수 있으므로 설정을 제공한다.

```text
핀 번호 표시
○ 항상
○ 확대했을 때만
○ 숨김
```

핀 이름도 동일하게 설정 가능하다.

---

# 25. 부품 이름

각 부품에는 사용자가 이름을 붙일 수 있다.

예:

```text
74HC161
PC LOW
```

```text
74HC161
PC HIGH
```

```text
74HC157
MUX LOW
```

```text
CDP1824CE
RAM #1
```

부품명을 지정하지 않아도 사용 가능하다.

---

# 26. LED

지원 색:

* Red
* Green
* Blue
* Yellow
* White
* Orange

실제 LED처럼 2핀 형태.

```text
A+
K-
```

극성 표시.

---

# 27. 저항

기본 저항:

* 220Ω
* 330Ω
* 1kΩ
* 2.2kΩ
* 4.7kΩ
* 10kΩ
* 22kΩ

그리고:

```text
사용자 지정 저항
```

을 추가한다.

사용자가 원하는 값을 입력:

```text
470Ω
100kΩ
```

---

# 28. 가변저항

지원:

* 10kΩ Potentiometer
* 사용자 지정 Potentiometer

3핀으로 표현한다.

---

# 29. 커패시터

기본 제공:

* 10nF
* 0.1µF 세라믹
* 47µF 전해
* 470µF 전해

그리고:

```text
사용자 지정 커패시터
```

지원.

전해 커패시터는 + / - 극성을 표시한다.

---

# 30. 버튼 / 스위치

지원:

* Push Button
* Reset Button
* Slide Switch
* Toggle Switch
* DIP Switch

실제 배치용일 뿐 동작 시뮬레이션은 하지 않는다.

---

# 31. LCD

지원:

```text
HD44780 20×4 LCD
```

16핀을 모두 표현한다.

```text
1 VSS
2 VDD
3 VO
4 RS
5 RW
6 E
7 D0
8 D1
9 D2
10 D3
11 D4
12 D5
13 D6
14 D7
15 LED+
16 LED-
```

화면 내용은 시뮬레이션하지 않는다.

---

# 32. PS/2 관련 부품

지원:

* Mini-DIN 6핀
* NPN Transistor
* PS/2 Connector

---

# 33. 전원 관련 부품

지원:

* +5V Terminal
* GND Terminal
* DC Jack
* Power Connector

필요하면 사용자가 직접 라벨도 붙일 수 있다.

---

# 34. 범용 부품

예상하지 못한 부품도 추가할 수 있도록:

```text
Generic DIP8
Generic DIP14
Generic DIP16
Generic DIP18
Generic DIP20
Generic DIP28

Generic 2-pin
Generic 3-pin
Generic 4-pin
```

도 제공한다.

이름과 핀 이름을 사용자가 직접 수정할 수 있게 한다.

---

# 35. 점퍼선 연결

핀이든 브레드보드 구멍이든 클릭해서 연결한다.

```text
첫 번째 점 클릭
↓
배선 시작
↓
필요하면 빈 공간 클릭
↓
꺾이는 지점 생성
↓
두 번째 점 클릭
↓
배선 완료
```

---

# 36. 자유로운 점퍼선 경로

팅커캐드처럼 점퍼선을 여러 번 꺾을 수 있다.

예:

```text
●──────┐
       │
       └─────┐
             │
             └────●
```

꺾이는 점을 드래그해서 위치 변경 가능.

---

# 37. 자동 배선과 수동 배선

기본은 수동 경로 지정.

선택 옵션:

```text
○ 직접 경로
○ 직각 자동 경로
```

자동 경로는 보기 편하게만 정리하고 실제 전기적 의미는 없다.

---

# 38. 점퍼선 색상

점퍼선 색은 사용자가 자유롭게 선택한다.

기본 팔레트:

```text
Red
Black
Green
Yellow
Blue
Orange
White
Purple
Gray
Brown
```

그리고 사용자 지정 색상도 가능.

어떤 색을 사용해도 프로그램은 경고하지 않는다.

---

# 39. 최근 사용 색상

실제 작업할 때 빠르게 선택할 수 있도록:

```text
최근 색
🟥 ⬛ 🟨 🟩 🟦
```

표시.

---

# 40. 점퍼선 두께

화면 보기 편의를 위해:

```text
얇게
보통
굵게
```

설정 가능.

실제 선 종류를 의미하는 것은 아니다.

---

# 41. 선 선택

점퍼선을 클릭하면 선택 가능.

선택 후:

* 삭제
* 색상 변경
* 경로 변경
* 꺾이는 점 추가
* 꺾이는 점 삭제

가능.

---

# 42. 선 위에 마우스 올리기

Hover하면 연결 양 끝을 강조한다.

예:

```text
74HC161 Pin14
↔
74HC157 Pin2
```

단순 정보 표시만 한다.

오류 여부는 판단하지 않는다.

---

# 43. 연결점 강조

핀이나 브레드보드 구멍을 클릭하면 해당 점에 연결된 점퍼선을 강조할 수 있다.

복잡한 회로에서 매우 중요하다.

예:

```text
Pin14 클릭
→ 관련 점퍼선만 선명하게
→ 나머지 선은 반투명
```

---

# 44. 선 숨기기

복잡한 회로를 보기 쉽게:

```text
모든 선 숨김
모든 선 표시
선택한 선만 표시
```

지원.

---

# 45. 색상별 선 필터

예:

```text
☑ 빨강
☑ 검정
☑ 초록
☑ 노랑
☑ 파랑
☑ 주황
☑ 흰색
```

선택한 색의 점퍼선만 표시할 수 있다.

이것도 검사 기능이 아니라 단순 보기 기능이다.

---

# 46. 부품 숨기기

일부 부품을 임시로 숨길 수 있다.

```text
Hide
Show
```

복잡한 배선을 확인하는 데 사용.

---

# 47. 레이어

선택적으로 레이어 기능을 제공한다.

예:

```text
Layer 1: Breadboard
Layer 2: IC
Layer 3: Power wires
Layer 4: Other wires
Layer 5: Labels
```

하지만 전자 CAD처럼 복잡하게 만들 필요는 없다.

---

# 48. 이름표

캔버스에 자유롭게 텍스트를 추가할 수 있다.

예:

```text
PC
RAM
ALU
CONTROL LOGIC
CLOCK
```

브레드보드 위에 영역 이름을 표시할 수 있다.

---

# 49. 화살표 / 간단한 도형

편의를 위해:

* Text
* Arrow
* Rectangle

정도만 지원하면 좋다.

예:

```text
┌───────────┐
│ PC AREA   │
└───────────┘
```

회로 설명용이다.

---

# 50. 속성 패널

부품 선택 시 오른쪽에 표시:

```text
Component

Type
74HC161

Name
PC LOW

Position
X: 1240
Y: 380

Rotation
0°

Breadboard
BB1

Note
...
```

---

# 51. 핀 정보

IC 핀에 마우스를 올리면:

```text
74HC161
Pin 14
QA
```

정도만 표시한다.

어디에 연결되어 있는지도 볼 수 있다.

```text
Connected to:
Jumper #34
```

---

# 52. 메모

부품마다 자유 메모 가능.

예:

```text
PC 하위 카운터
실제 브레드보드 왼쪽에서 두 번째 IC
```

또는:

```text
이 선 나중에 다시 확인
```

프로그램이 메모 내용을 해석하지 않는다.

---

# 53. 브레드보드 구멍 좌표

각 구멍에는 내부적으로 정확한 주소를 준다.

예:

```text
BB1:A:1
BB1:A:2
BB1:E:15
BB1:F:15
BB1:RTP:22
```

사용자 화면에서는 필요할 때만 표시한다.

---

# 54. 부품이 브레드보드에 꽂히는 방식

IC 핀을 실제 구멍에 맞춰 배치한다.

예:

```text
74HC161 Pin1 → BB1:E20
74HC161 Pin16 → BB1:F20
```

이 관계를 파일에 저장한다.

실제 배치를 재현하기 위해 필요하다.

---

# 55. 브레드보드 위 부품 Snap

DIP IC를 브레드보드 중앙 홈 위에 놓으면 자동으로 구멍에 맞춰준다.

잘못 놓고 싶다면 Snap을 끌 수도 있다.

---

# 56. 겹침 허용

현실에서 부품이나 선이 시각적으로 겹칠 수 있으므로 프로그램도 필요 이상으로 막지 않는다.

단순 편집 도구이기 때문에 사용자의 배치를 제한하지 않는다.

---

# 57. 저장

가장 중요한 기능 중 하나다.

```text
Ctrl + S
```

지원.

프로젝트를 파일로 저장한다.

예:

```text
makecomputer_2026-09-19.json
```

---

# 58. 자동 저장

브라우저 로컬 저장을 이용해 자동 저장한다.

예:

```text
30초마다 자동 저장
```

또는 변경 시 자동 저장.

브라우저가 꺼져도 작업 복구 가능하게 한다.

---

# 59. 저장 파일 내용

파일에는 최소한 다음을 저장한다.

```text
프로젝트 정보
캔버스 위치
줌
브레드보드
부품
부품 위치
부품 회전
부품 이름
점퍼선
점퍼선 색
점퍼선 꺾임 좌표
라벨
메모
설정
```

---

# 60. JSON 구조 예시

```json
{
  "projectName": "MAKECOMPUTER",

  "view": {
    "zoom": 1.2,
    "panX": -420,
    "panY": 180
  },

  "boards": [
    {
      "id": "bb1",
      "name": "PC BOARD",
      "x": 200,
      "y": 300
    }
  ],

  "components": [
    {
      "id": "c1",
      "type": "74HC161",
      "name": "PC LOW",
      "x": 520,
      "y": 430,
      "rotation": 0
    }
  ],

  "wires": [
    {
      "id": "w1",
      "color": "#f2c94c",

      "from": {
        "type": "componentPin",
        "componentId": "c1",
        "pin": 14
      },

      "to": {
        "type": "breadboardHole",
        "boardId": "bb1",
        "row": "A",
        "column": 32
      },

      "points": [
        {
          "x": 720,
          "y": 400
        },
        {
          "x": 850,
          "y": 400
        }
      ]
    }
  ]
}
```

---

# 61. 불러오기

저장한 JSON 파일을 다시 열면 정확히 이전 모습으로 복구한다.

포함:

* 화면 위치
* 확대 배율
* 모든 브레드보드
* 부품
* 점퍼선
* 꺾이는 지점
* 메모
* 라벨

---

# 62. 다른 이름으로 저장

```text
Save As
```

지원.

예:

```text
PC_only.json
PC_MUX_connected.json
PC_MUX_MAR.json
```

제작 단계마다 버전을 만들 수 있다.

---

# 63. 작업 이력 저장

선택 기능으로:

```text
Snapshot 만들기
```

지원.

예:

```text
Snapshot 1
클럭 완성

Snapshot 2
PC 추가

Snapshot 3
MUX 추가
```

과거 상태로 돌아가는 용도다.

---

# 64. 이미지 내보내기

전체 회로를 PNG로 저장할 수 있게 한다.

```text
PNG 내보내기
```

옵션:

```text
현재 화면
전체 작업 공간
선택 영역
```

---

# 65. 고해상도 이미지

전체 회로가 커져도 핀 번호가 보이도록 고해상도 출력 지원.

예:

```text
1x
2x
4x
```

---

# 66. 그리드

작업 공간 배경에 선택적으로 격자를 표시한다.

```text
☑ Grid 표시
```

Zoom 수준에 따라 자동으로 간격을 조절한다.

---

# 67. 미니맵

회로가 매우 커지면 오른쪽 아래에 미니맵을 표시한다.

```text
┌────────────┐
│ ■■■        │
│    ■■■■    │
│        ■■  │
└────────────┘
```

현재 보고 있는 영역을 사각형으로 표시한다.

---

# 68. 전체 보기

단축키:

```text
F
```

또는 버튼:

```text
전체 보기
```

모든 브레드보드와 부품이 화면에 들어오게 자동으로 Zoom/Pan 한다.

---

# 69. 선택 항목으로 이동

부품 검색 후:

```text
PC LOW
```

를 클릭하면 해당 위치로 화면이 이동하고 확대한다.

---

# 70. 부품 목록 패널

현재 프로젝트에 사용 중인 모든 부품을 별도 목록으로 볼 수 있다.

예:

```text
PC LOW          74HC161
PC HIGH         74HC161
MUX LOW         74HC157
MUX HIGH        74HC157
MAR             74HC273
```

클릭하면 화면에서 해당 부품 위치로 이동.

---

# 71. 선 목록

필요하면 모든 점퍼선을 목록으로 볼 수 있다.

```text
Wire 001
Wire 002
Wire 003
...
```

선택하면 해당 점퍼선 강조.

---

# 72. 단축키

기본 단축키:

```text
Ctrl + S          저장
Ctrl + Z          실행 취소
Ctrl + Y          다시 실행
Ctrl + C          복사
Ctrl + V          붙여넣기

Delete            삭제
R                 회전
Esc               현재 작업 취소

Ctrl + Wheel      확대/축소
Middle Drag       화면 이동

F                 전체 보기
1                 100% Zoom
```

---

# 73. Esc 동작

`Esc`는 현재 진행 중인 작업을 취소한다.

예:

```text
점퍼선 연결 중
→ Esc
→ 연결 취소
```

```text
부품 이동 중
→ Esc
→ 원래 위치
```

---

# 74. 우클릭 메뉴

부품:

```text
복사
삭제
회전
이름 변경
맨 앞으로
맨 뒤로
```

점퍼선:

```text
색 변경
꺾임점 추가
삭제
```

브레드보드:

```text
이름 변경
복제
삭제
```

---

# 75. 작업 잠금

실수로 완성된 부분을 움직이지 않도록:

```text
Lock
```

지원.

예:

```text
PC 영역 Lock
```

하면 클릭은 가능하지만 이동/삭제는 막는다.

Unlock하면 다시 편집 가능하다.

---

# 76. 선택 잠금

브레드보드를 완성하면 통째로 잠글 수도 있다.

회로가 커질수록 중요한 기능이다.

---

# 77. 성능 목표

컴퓨터 전체를 표현해야 하므로 최소:

* 브레드보드 15개
* 부품 150개 이상
* 점퍼선 1000개 이상

정도에서도 화면 이동과 확대/축소가 부드러워야 한다.

---

# 78. 렌더링 방식

추천:

```text
HTML
CSS
JavaScript / TypeScript
SVG
```

특히 점퍼선은 SVG가 적합하다.

```text
SVG Path
```

를 사용하면:

* 선 꺾기
* 선 선택
* 확대/축소
* 선 색 변경

구현이 쉽다.

---

# 79. 데이터와 화면 분리

중요하다.

화면에 보이는 SVG 자체를 저장하지 않는다.

실제 프로젝트 데이터:

```text
Board
Component
Pin
Wire
Position
Rotation
```

를 JSON으로 저장한다.

화면은 이 데이터를 기반으로 다시 그린다.

---

# 80. 최종 핵심 기능

이 프로그램에서 정말 중요한 기능은 다음이다.

```text
1. 실제 브레드보드와 비슷한 화면

2. 모든 사용하는 부품 제공

3. 정확한 IC 핀 위치

4. 실제 위치 그대로 부품 배치

5. 실제 연결 그대로 점퍼선 그리기

6. 점퍼선 자유로운 색 선택

7. 점퍼선 자유로운 꺾기

8. 확대 / 축소

9. 자유로운 화면 이동

10. 실행 취소 / 다시 실행

11. 복사 / 붙여넣기

12. 회전

13. 다중 선택

14. 숨기기 / 필터

15. 검색

16. 저장 / 불러오기

17. 자동 저장

18. PNG 내보내기

19. 큰 프로젝트에서도 좋은 성능
```

---

# 81. 최종 사용 모습

실제로 PC 부분을 제작했다면:

```text
현실

NE555
│
├── 74HC161 PC LOW
│
└── 74HC161 PC HIGH
```

웹에서 브레드보드를 배치한다.

실제 위치에:

```text
NE555
74HC161
74HC161
```

을 꽂는다.

그 후 현실의 점퍼선을 보면서:

```text
선 하나 연결
↓
웹에서도 하나 연결

다음 선 연결
↓
웹에서도 하나 연결
```

을 반복한다.

완료하면:

```text
Ctrl + S
```

로 저장한다.

끝이다.

---

# 82. 프로젝트 최종 정의

**MAKECOMPUTER Breadboard Digitalizer**는 회로 설계 프로그램도 아니고 시뮬레이터도 아니다.

> 현실에서 만들어진 브레드보드 회로를 가능한 한 정확하고 편하게 디지털 형태로 복제하는 전용 편집기다.

프로그램은 사용자에게

```text
이 연결은 맞다.
이 연결은 틀렸다.
이 색은 잘못됐다.
```

라고 판단하지 않는다.

오직:

> **“현실에서 보이는 그대로 디지털로 옮길 수 있게 하는 것”**

이 프로그램의 유일한 목적이다.
