# 8비트 컴퓨터 만들기

> 이 문서는 우리가 실제로 제작하는 **8비트 브레드보드 컴퓨터의 현재 확정 구조**를 설명한다.
> 현재 메모리 설계는 **CDP1824CE 5개를 모두 사용하여 총 160 byte**를 구성하는 것으로 확정한다.

## 목차

1. [먼저 알아둘 기본 용어](#1-먼저-알아둘-기본-용어)
2. [컴퓨터의 전체 구조](#2-컴퓨터의-전체-구조)
3. [사용하는 핵심 IC](#3-사용하는-핵심-ic)
4. [클럭 - NE555](#4-클럭---ne555)
5. [PC와 Microstep Counter - 74HC161](#5-pc와-microstep-counter---74hc161)
6. [메모리 - CDP1824CE 5개](#6-메모리---cdp1824ce-5개)
7. [BUS - 74HC245](#7-bus---74hc245)
8. [레지스터 - 74HC273](#8-레지스터---74hc273)
9. [ALU - 74HC283 + 74HC86](#9-alu---74hc283--74hc86)
10. [MUX - 74HC157](#10-mux---74hc157)
11. [디코더와 제어회로](#11-디코더와-제어회로)
12. [전체 구조](#12-전체-구조)
13. [현재 확정된 구조](#13-현재-확정된-구조)

---

# 1. 먼저 알아둘 기본 용어

## Bit와 Byte

컴퓨터 내부의 기본 정보는 `0`과 `1`이다.

```text
0 = LOW
1 = HIGH
```

`0` 또는 `1` 하나를 **1 bit**라고 하고, 8개의 bit를 묶으면 **1 byte**다.

```text
8 bit = 1 byte
```

우리 컴퓨터는 주요 데이터 경로가 8비트이므로 한 번에 주로 다음과 같은 값을 처리한다.

```text
10100101
```

---

## CPU

**CPU(Central Processing Unit)**는 명령어를 읽고, 데이터를 이동시키고, 계산하고, 다른 장치를 제어하는 중심 장치다.

일반 컴퓨터에서는 CPU가 하나의 복잡한 칩이지만 우리는 다음 기능들을 여러 IC로 직접 만든다.

```text
Clock
PC
RAM
Register
ALU
BUS
Instruction Decoder
Microstep Decoder
Control Logic
```

---

## Address

**Address(주소)**는 메모리의 어느 위치를 사용할지 나타내는 번호다.

우리 컴퓨터는 PC와 MAR가 8비트이므로 주소 자체는

```text
0 ~ 255
```

까지 표현할 수 있다.

현재 실제 RAM은 그중

```text
0 ~ 159
```

을 사용한다.

---

# 2. 컴퓨터의 전체 구조

가장 단순하게 보면 다음과 같다.

```text
             프로그램 / 데이터
                    │
                    ▼
                  RAM
                    │
                    ▼
================ DATA BUS ================
       │              │              │
       ▼              ▼              ▼
   Register          ALU            OUT
       │              │
       └───────┬──────┘
               ▼
              결과
```

CPU가 언제 어떤 일을 할지는 다음 구조가 결정한다.

```text
                 CLOCK
                   │
                   ▼
           Microstep Counter
                   │
                   ▼
          Microstep Decoder
                   │
                   ├──────────┐
                   │          │
IR ─► Instruction Decoder     │
                   │          │
                   └────┬─────┘
                        ▼
                  Control Logic
                        │
          ┌─────────────┼─────────────┐
          ▼             ▼             ▼
         PC            RAM         Register
```

---

# 3. 사용하는 핵심 IC

현재 기본 설계에서 실제로 사용하는 핵심 IC 수량이다.

| 부품 | 사용 수량 | 역할 |
|---|---:|---|
| **NE555** | 1 | 가변 클럭 생성 |
| **74HC161** | 3 | 8비트 PC 2개 + Microstep Counter 1개 |
| **CDP1824CE** | **5** | 160 byte RAM |
| **74HC283N** | 2 | 8비트 덧셈 |
| **74HC86** | 2 | 8비트 뺄셈용 XOR |
| **74HC273** | 4 | A, IR, MAR, OUT 레지스터 |
| **74HC245** | 3 | DATA BUS 연결/차단 |
| **74HC157** | 4 | 주소/데이터 경로 선택 |
| **74HC138** | **2** | Microstep Decoder + RAM Bank Decoder |
| **74HC139** | 1 | Instruction Decoder |
| **74HC08** | 3 | AND 논리 |
| **74HC32** | 3 | OR 논리 |
| **74HC04** | 2 | NOT 논리 |

> 실제 확보 수량은 `부품목록.md`를 기준으로 한다.

---

# 4. 클럭 - NE555

## 클럭이란?

클럭은 컴퓨터가 같은 박자에 맞춰 동작하도록 만드는 반복 신호다.

```text
HIGH ────┐      ┌──────┐      ┌──────
         │      │      │      │
LOW  ────┴──────┘      └──────┘
```

NE555와 가변저항을 사용하여 클럭 속도를 조절한다.

```text
가변저항
   │
   ▼
 NE555 ───► CLOCK
```

초기 제작과 디버깅에서는 느린 클럭을 사용하면 PC, BUS, Microstep 등의 변화를 LED로 눈으로 확인하기 쉽다.

74HC161과 74HC273 같은 순차논리 IC는 클럭의 특정 엣지에서 값을 증가시키거나 저장한다.

---

# 5. PC와 Microstep Counter - 74HC161

## 8비트 Program Counter

74HC161 하나는 4비트 카운터다.

두 개를 직렬로 연결해서 8비트 PC를 만든다.

```text
74HC161 #1 : PC3 PC2 PC1 PC0
74HC161 #2 : PC7 PC6 PC5 PC4

합치면
PC7 PC6 PC5 PC4 PC3 PC2 PC1 PC0
```

따라서 PC는

```text
0 ~ 255
```

까지 표현할 수 있다.

PC는 **다음에 읽을 바이트의 주소**를 기억한다.

---

## Microstep Counter

세 번째 74HC161은 명령어 내부의 작은 실행 단계를 센다.

아래 3비트를 사용한다.

```text
000 → T0
001 → T1
010 → T2
011 → T3
100 → T4
101 → T5
110 → T6
111 → T7
```

즉 하나의 명령어를 최대 8개의 작은 단계로 나눠 실행할 수 있다.

---

# 6. 메모리 - CDP1824CE 5개

## CDP1824CE 하나

CDP1824CE 하나는

```text
32 × 8 bit
```

RAM이다.

한 주소에 8비트 = 1바이트를 저장하므로

```text
32 byte
```

다.

우리는 이 RAM을 **5개 모두 사용**한다.

```text
32 byte × 5 = 160 byte
```

---

## 왜 RAM 하나당 주소선이 5개인가?

32개의 위치 중 하나를 고르려면 5비트가 필요하다.

```text
00000 = 0
00001 = 1
...
11111 = 31
```

그래서 ADDRESS BUS의 하위 5비트

```text
A4 A3 A2 A1 A0
```

를 RAM 5개 모두의 내부 주소 입력에 공통으로 연결한다.

---

## RAM 5개 중 하나 선택하기

8비트 주소를 다음처럼 나눈다.

```text
A7 A6 A5 | A4 A3 A2 A1 A0
---------+---------------
 RAM 선택 | RAM 내부 주소
```

상위 3비트 `A7~A5`를 두 번째 **74HC138**에 넣는다.

```text
A7 A6 A5
   │
   ▼
74HC138
RAM Bank Decoder
   │
   ├── Y0 ─► RAM #1
   ├── Y1 ─► RAM #2
   ├── Y2 ─► RAM #3
   ├── Y3 ─► RAM #4
   ├── Y4 ─► RAM #5
   ├── Y5 ─► 현재 미사용
   ├── Y6 ─► 현재 미사용
   └── Y7 ─► 현재 미사용
```

74HC138은 3비트 값에 따라 8개의 출력 중 하나를 선택하므로, 한 순간에 원하는 RAM Bank 하나만 활성화할 수 있다.

CDP1824CE에는 RAM 선택을 위한 `CS(Chip Select)` 입력이 있으므로 RAM Bank Decoder의 선택 신호를 이 경로에 사용한다. 실제 배선 시에는 **CDP1824CE와 74HC138의 Active-Low 극성을 데이터시트 핀아웃과 함께 최종 확인**한다.

---

## 메모리 맵

| A7 A6 A5 | RAM | 전체 주소 | 내부 주소 |
|---|---|---:|---:|
| `000` | RAM #1 | 0~31 | 0~31 |
| `001` | RAM #2 | 32~63 | 0~31 |
| `010` | RAM #3 | 64~95 | 0~31 |
| `011` | RAM #4 | 96~127 | 0~31 |
| `100` | RAM #5 | 128~159 | 0~31 |
| `101` | 미사용 | 160~191 | - |
| `110` | 미사용 | 192~223 | - |
| `111` | 미사용 | 224~255 | - |

예를 들어 주소 `133`은 다음과 같다.

```text
133 = 10000101₂

A7~A5 = 100 → RAM #5
A4~A0 = 00101 → RAM 내부 주소 5
```

따라서

```text
전체 주소 133
= RAM #5의 내부 주소 5
```

이다.

---

## 남는 주소 공간

PC와 MAR는 8비트이므로 256개의 주소를 표현할 수 있지만 RAM은 160 byte다.

```text
0   ~ 159 : RAM
160 ~ 255 : 현재 미사용
```

이 96개의 주소는 나중에 **Memory-Mapped I/O** 같은 우리만의 확장 기능에 사용할 수 있다.

예시:

```text
160~191 : 외부 입출력
192~223 : Debug / Status
224~239 : 센서 또는 확장 장치
240~255 : System Control
```

이 배치는 아직 확정하지 않고 주소 영역만 예약 가능 상태로 둔다.

---

## PC와 MAR 중 어떤 주소를 RAM에 보낼까?

프로그램을 순서대로 읽을 때는 PC를 사용하고, 명령어가 지정한 다른 메모리 위치에 접근할 때는 MAR를 사용한다.

```text
8bit PC ───────┐
               │
               ▼
          74HC157 ×2 ───► 8bit ADDRESS BUS ───► RAM System
               ▲
               │
8bit MAR ──────┘
```

---

# 7. BUS - 74HC245

## DATA BUS

DATA BUS는 CPU 내부에서 8비트 값을 주고받는 공용 통로다.

```text
BUS7
BUS6
BUS5
BUS4
BUS3
BUS2
BUS1
BUS0
```

BUS는 데이터를 저장하지 않고 전달만 한다.

---

## Bus Contention

두 장치가 같은 BUS에 동시에 서로 다른 값을 출력하면 안 된다.

```text
RAM : 00000000 ──┐
                  ├── BUS 충돌
ALU : 11111111 ──┘
```

그래서 74HC245의 `/OE`를 이용해 필요한 출력 경로 하나만 BUS를 구동하도록 제어한다.

`High-Z` 상태에서는 장치가 BUS에서 사실상 손을 떼므로 여러 장치를 같은 버스에 연결할 수 있다.

---

## 사용하는 74HC245 3개

```text
#1 : RAM ↔ DATA BUS
#2 : A/ALU → DATA BUS
#3 : 입력 스위치 → DATA BUS
```

입력 스위치 경로는 프로그램이나 데이터를 수동으로 RAM에 입력하는 PROGRAM 모드에서 사용할 수 있다.

---

# 8. 레지스터 - 74HC273

74HC273 하나에는 8개의 D Flip-Flop이 들어 있어 8비트 값을 저장할 수 있다.

현재 4개를 사용한다.

| 레지스터 | 역할 |
|---|---|
| **A Register** | 계산할 값 저장 |
| **IR** | 현재 명령어 저장 |
| **MAR** | RAM에 접근할 주소 저장 |
| **OUT Register** | 외부 출력값 저장 |

```text
RAM ─► BUS ─► A
RAM ─► BUS ─► IR
RAM ─► BUS ─► MAR
A/ALU ─► BUS ─► OUT
```

### 주의: 74HC273의 Load Enable

74HC273에는 전용 Load Enable 핀이 없다.

따라서 A, IR, MAR, OUT 중 필요한 레지스터만 특정 클럭에서 저장하도록 만드는 **Clock Enable/게이팅 방식은 실제 배선 설계에서 반드시 별도로 해결**해야 한다.

이 부분은 현재 컴퓨터의 중요한 세부 설계 과제다.

---

# 9. ALU - 74HC283 + 74HC86

## 8비트 덧셈

74HC283은 4비트 가산기다.

두 개를 캐스케이드한다.

```text
A3~A0 ─┐                    A7~A4 ─┐
        ▼                           ▼
   74HC283 #1 ── Carry ──► 74HC283 #2
        ▲                           ▲
B3~B0 ──┘                    B7~B4 ─┘
```

이렇게 8비트 덧셈을 만든다.

---

## 뺄셈

2의 보수를 이용한다.

```text
A - B
=
A + NOT(B) + 1
```

74HC86 XOR 두 개로 B의 8비트를 선택적으로 반전한다.

```text
SUB = 0 → B 그대로 → ADD
SUB = 1 → B 반전 + Cin 1 → SUB
```

---

# 10. MUX - 74HC157

74HC157 하나는 4채널 2:1 MUX다.

8비트 값을 선택하려면 두 개가 필요하다.

## 주소 MUX

74HC157 두 개를 이용해 PC와 MAR 중 하나를 선택한다.

```text
PC ──┐
     ├── 74HC157 ×2 ─► ADDRESS BUS
MAR ─┘
```

## A / ALU MUX

다른 74HC157 두 개를 이용해 BUS로 보낼 값의 출처를 선택한다.

```text
A Register ─┐
            ├── 74HC157 ×2 ─► 74HC245 ─► DATA BUS
ALU Result ─┘
```

---

# 11. 디코더와 제어회로

## 74HC138 #1 — Microstep Decoder

Microstep Counter의 하위 3비트를 받아 `T0~T7`을 구분한다.

```text
Microstep Counter
      │ 3bit
      ▼
  74HC138 #1
      │
T0 T1 T2 T3 T4 T5 T6 T7
```

---

## 74HC138 #2 — RAM Bank Decoder

ADDRESS BUS의 상위 3비트를 받아 5개의 RAM 중 하나를 고른다.

```text
A7 A6 A5
   │
   ▼
74HC138 #2
   │
RAM1 RAM2 RAM3 RAM4 RAM5
```

즉 이번 RAM 확장으로 **74HC138을 총 2개 기본 사용**한다.

---

## 74HC139 — Instruction Decoder

IR의 상위 3비트를 Opcode로 사용한다.

```text
IR7 IR6 IR5 = Opcode
```

3비트이므로 최대 8개의 기본 명령어를 구분한다.

현재 명령어의 기본 형태는

```text
bit7 bit6 bit5 | bit4 bit3 bit2 bit1 bit0
---------------+--------------------------
    OPCODE     |       Reserved
```

이다.

주소가 필요한 명령어는 다음 바이트를 주소로 사용한다.

```text
[명령어 byte]
[주소 byte]
```

RAM이 160 byte로 확장되었으므로 주소 Operand는 **8비트 한 바이트 전체**를 사용한다.

---

## AND / OR / NOT

74HC08, 74HC32, 74HC04를 이용해 현재 명령어와 마이크로스텝을 조합하여 실제 제어 신호를 만든다.

```text
Instruction Decoder ───┐
                       ├── AND / OR / NOT ─► Control Signal
Microstep Decoder ─────┘
```

예를 들어

```text
LDA 명령
AND
T4 단계
```

일 때만 특정 RAM 읽기 신호와 A Register 저장 신호를 발생시키는 식으로 구성한다.

---

# 12. 전체 구조

```text
                              NE555
                                │
                              CLOCK
                                │
              ┌─────────────────┴──────────────────┐
              ▼                                    ▼
       74HC161 ×2                           74HC161 ×1
        8bit PC                          Microstep Counter
              │                                    │
              │                                    ▼
              │                              74HC138 #1
              │                                T0~T7
              │                                    │
              │                              Control Logic
              │                                    │
              ▼                                    │
            8bit PC                                │
              │                                    │
              ├────────────┐                       │
              │            │                       │
              │        8bit MAR                    │
              │        74HC273                     │
              │            │                       │
              └──────┬─────┘                       │
                     ▼                             │
                74HC157 ×2                         │
                Address MUX                        │
                     │                             │
                     ▼                             │
================ 8bit ADDRESS BUS ==================
 A7 A6 A5                         A4 A3 A2 A1 A0
     │                                  │
     ▼                                  │
74HC138 #2                              │
RAM Bank Decoder                        │
     │                                  │
     ├── RAM #1 ◄───────────────────────┤
     ├── RAM #2 ◄───────────────────────┤
     ├── RAM #3 ◄───────────────────────┤
     ├── RAM #4 ◄───────────────────────┤
     └── RAM #5 ◄───────────────────────┘

        CDP1824CE ×5 = 160 byte
                     │
                     ▼
                 74HC245 #1
                     │
                     ▼
================= 8bit DATA BUS ===================
       │             │             │             │
       ▼             ▼             ▼             ▼
      IR            MAR            A            OUT
   74HC273        74HC273       74HC273       74HC273
       │                           │             │
       ▼                           │             ▼
   74HC139                        ALU           LED
Instruction Decoder        74HC283×2 + HC86×2
                                   │
                         A ─────┐   │
                               ▼   ▼
                              74HC157 ×2
                                   │
                               74HC245 #2
                                   │
                                DATA BUS

입력 스위치 8bit
      │
   74HC245 #3
      │
   DATA BUS
```

---

# 13. 현재 확정된 구조

## 데이터 폭

```text
8 bit
```

## Program Counter

```text
74HC161 ×2
8 bit
0~255 표현 가능
```

## Microstep Counter

```text
74HC161 ×1
T0~T7
```

## RAM

```text
CDP1824CE ×5
32 byte ×5
= 160 byte
```

메모리 맵:

```text
0   ~ 31  → RAM #1
32  ~ 63  → RAM #2
64  ~ 95  → RAM #3
96  ~ 127 → RAM #4
128 ~ 159 → RAM #5
160 ~ 255 → 현재 미사용 / 확장 예약
```

## RAM 선택

```text
A7~A5
  │
74HC138 #2
  │
RAM #1~#5 Select
```

RAM 내부 주소:

```text
A4~A0
```

## 레지스터

```text
74HC273 #1 → A Register
74HC273 #2 → IR
74HC273 #3 → MAR
74HC273 #4 → OUT Register
```

## ALU

```text
74HC283 ×2
74HC86 ×2
ADD / SUB
```

## 주소 선택

```text
PC ──┐
     ├── 74HC157 ×2 ─► 8bit ADDRESS BUS
MAR ─┘
```

## 명령어

```text
Opcode = 3 bit
기본 명령어 최대 8종류
Operand 주소 = 필요 시 다음 1 byte
```

## Decoder

```text
74HC138 #1 → Microstep T0~T7
74HC138 #2 → RAM Bank #1~#5
74HC139    → Instruction Opcode
```

## 현재 가장 중요한 미해결 세부 설계

1. 74HC273에는 Load Enable이 없으므로 각 레지스터가 필요한 클럭에서만 저장하도록 하는 회로를 확정해야 한다.
2. RAM Bank Decoder와 CDP1824CE의 실제 CS 극성/핀 연결은 데이터시트를 기준으로 최종 배선해야 한다.
3. 주소 `160~255`는 현재 비워 두고, 이후 우리만의 Memory-Mapped I/O 또는 디버깅 기능을 설계할 때 사용한다.

---

## 최종 한 줄 요약

> **8비트 PC와 DATA BUS, 4개의 8비트 레지스터, ADD/SUB ALU, 5개의 CDP1824CE로 구성된 160-byte RAM, 두 개의 74HC138을 이용한 Microstep/RAM Bank 디코딩을 갖는 브레드보드 컴퓨터를 제작한다.**
