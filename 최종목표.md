# 최종 목표 — 키보드와 텍스트 터미널을 갖춘 8비트 브레드보드 컴퓨터

> 이 문서는 `makecomputer` 프로젝트가 최종적으로 어떤 형태의 컴퓨터를 목표로 하는지 정리한 문서다.
>
> 핵심 목표는 **현재 제작 중인 8비트 브레드보드 CPU를 먼저 완성한 뒤**, CPU 본체를 중심으로 **PS/2 키보드, 텍스트 디스플레이, 자동 부팅 ROM, Monitor 프로그램을 주변 장치 형태로 확장**하여 실제로 사용자가 명령을 입력하고 결과를 확인할 수 있는 독립형 컴퓨터를 만드는 것이다.

---

## 1. 최종 한 줄 목표

**Arduino나 Raspberry Pi가 CPU 역할을 대신하지 않고, TTL 논리회로로 직접 만든 8비트 CPU가 PS/2 키보드 입력을 받아 텍스트 화면에 결과를 출력하며, 전원을 켜면 ROM의 Monitor 프로그램이 자동으로 실행되는 독립형 브레드보드 컴퓨터를 제작한다.**

---

## 2. 기본 제작 원칙

이 프로젝트는 처음부터 키보드나 LCD를 붙여 만드는 것이 아니라 다음 순서로 진행한다.

```text
1. 기존 8비트 CPU 완성
        ↓
2. CPU 단독 동작 검증
        ↓
3. Memory-Mapped I/O 확장
        ↓
4. 텍스트 LCD 연결
        ↓
5. PS/2 키보드 인터페이스 연결
        ↓
6. Boot ROM 연결
        ↓
7. Monitor Firmware 작성
        ↓
8. 독립형 터미널 컴퓨터 완성
```

즉 기존 컴퓨터를 버리고 새로 설계하는 것이 아니라,

> **기존 CPU를 본체로 완성하고 그 위에 주변장치를 하나씩 추가한다.**

는 방식으로 제작한다.

---

## 3. 최종 전체 구조

```text
                         ┌─────────────────┐
                         │   PS/2 Keyboard │
                         └────────┬────────┘
                                  │
                         Clock / Data
                                  │
                                  ▼
                    ┌────────────────────────┐
                    │ Keyboard Interface     │
                    │                        │
                    │ 74HC14                 │
                    │ 74HC164 ×2             │
                    │ 74HC74                 │
                    │ Scan Code Register     │
                    │ ASCII Lookup EEPROM    │
                    └───────────┬────────────┘
                                │
                                │ 8bit Data
                                ▼
============================ DATA BUS ============================
                  │                       │
                  │                       │
                  ▼                       ▼
        ┌──────────────────┐      ┌─────────────────┐
        │ 직접 만든 8bit   │      │ Boot ROM       │
        │ CPU              │      │ AT28C64B       │
        │                  │      │ Monitor 저장   │
        │ PC / IR / MAR    │      └─────────────────┘
        │ A / ALU / Control│
        └────────┬─────────┘
                 │
=================┼===============================================
                 │
                 ▼
        ┌──────────────────┐
        │ 20×4 Text LCD    │
        │ HD44780 호환     │
        │                  │
        │ MAKECOMPUTER/8   │
        │ SYSTEM READY     │
        │ > _              │
        └──────────────────┘
```

---

## 4. 기존 CPU는 먼저 그대로 완성한다

기본 CPU의 중심 구조는 현재 설계를 유지한다.

```text
Clock
PC
RAM 160 byte
A Register
IR
MAR
OUT Register
ALU
DATA BUS
Address MUX
Instruction Decoder
Microstep Decoder
Control Logic
```

현재 RAM 구조는 다음과 같다.

```text
RAM #1 :   0 ~  31
RAM #2 :  32 ~  63
RAM #3 :  64 ~  95
RAM #4 :  96 ~ 127
RAM #5 : 128 ~ 159
-------------------
총 RAM : 160 byte
```

PC와 MAR는 8비트이므로 주소 자체는 `0~255`를 표현할 수 있다.

따라서 남는 주소 공간

```text
160 ~ 255
```

을 주변장치용 **Memory-Mapped I/O 영역**으로 사용한다.

---

## 5. Memory-Mapped I/O

키보드와 LCD를 별도의 특수 명령으로만 제어하지 않고, CPU 입장에서는 메모리 주소처럼 접근하도록 만든다.

예상 I/O 주소는 다음과 같이 구성한다.

```text
0   ~ 159 : RAM

160       : KEY_STATUS
161       : KEY_DATA

162       : LCD_DATA
163       : LCD_COMMAND

164       : BOOT_CONTROL

165 ~ 255 : 향후 확장 영역
```

예를 들어 `KEY_DATA`를 읽으면 CPU는 키보드에서 들어온 ASCII 문자를 얻는다.

```text
LDA 161
```

LCD에 데이터를 보내는 경우에는 개념적으로 다음과 같이 동작한다.

```text
A = 출력할 문자
STA 162
```

즉 CPU 입장에서는 RAM에 접근하는 것과 유사하지만 주소 디코더가 실제 RAM 대신 해당 I/O 장치를 선택한다.

---

## 6. PS/2 키보드 입력

USB 키보드 대신 **PS/2 키보드**를 사용한다.

PS/2는 주로 다음 두 신호를 사용한다.

```text
CLOCK
DATA
```

키보드는 한 번에 ASCII 문자 하나를 병렬로 주는 것이 아니라 Scan Code를 직렬 데이터 형태로 전송한다.

따라서 키보드 인터페이스에서 다음 순서로 처리한다.

```text
PS/2 Keyboard
      │
      ▼
74HC14
신호 정리
      │
      ▼
74HC164 ×2
직렬 데이터 수신
      │
      ▼
Scan Code Register
      │
      ▼
ASCII Lookup EEPROM
      │
      ▼
ASCII 8bit
      │
      ▼
CPU DATA BUS
```

예:

```text
사용자가 A 키 입력
      ↓
PS/2 Scan Code
      ↓
ASCII 변환
      ↓
01000001
      ↓
'A'
```

Arduino를 키보드 해석용 보조 프로세서로 사용하지 않고 키보드 인터페이스 자체를 논리회로로 구성하는 것을 목표로 한다.

---

## 7. 텍스트 디스플레이

최종 출력 장치는 **20×4 HD44780 호환 병렬 문자 LCD**를 우선 사용한다.

예상 화면:

```text
┌────────────────────┐
│ MAKECOMPUTER/8     │
│ SYSTEM READY       │
│                    │
│ > _                │
└────────────────────┘
```

HD44780 계열 LCD는 문자 폰트를 자체적으로 가지고 있으므로 CPU가 문자 픽셀을 하나씩 그릴 필요가 없다.

CPU는 문자 코드와 LCD 제어 명령만 전달한다.

예:

```text
01000001 → 'A'
01000010 → 'B'
00110001 → '1'
```

최종적으로 키보드에서 입력한 문자를 CPU가 처리한 뒤 LCD에 출력하도록 한다.

---

## 8. 자동 부팅

기존 CDP1824CE RAM은 휘발성 메모리이므로 전원을 끄면 프로그램이 사라진다.

따라서 별도의 **AT28C64B EEPROM을 Boot ROM으로 사용**한다.

```text
POWER ON
   │
   ▼
RESET
   │
   ▼
BOOT_MODE = 1
   │
   ▼
Boot ROM 선택
   │
   ▼
Monitor Firmware 실행
```

PC는 평소처럼 주소 `0`에서 실행을 시작하지만 부팅 중에는 낮은 주소의 READ를 RAM 대신 Boot ROM이 응답하게 하는 **Boot ROM Overlay 방식**을 목표로 한다.

개념적으로:

```text
BOOT_MODE = 1
READ  낮은 주소 → Boot ROM
WRITE 낮은 주소 → 필요 시 RAM

BOOT_MODE = 0
READ/WRITE       → 정상 RAM
```

부팅이 완료된 후에는 RAM과 일반 I/O를 정상적으로 사용할 수 있도록 전환한다.

---

## 9. EEPROM 두 개의 역할

최종 확장에서는 AT28C64B를 서로 다른 목적으로 사용한다.

### EEPROM #1 — Keyboard Translation ROM

```text
PS/2 Scan Code
      ↓
AT28C64B #1
      ↓
ASCII
```

키보드 Scan Code를 CPU가 다루기 쉬운 문자 코드로 변환한다.

### EEPROM #2 — Boot ROM

```text
POWER ON
   ↓
AT28C64B #2
   ↓
Monitor Firmware
```

전원을 꺼도 Monitor 프로그램을 유지하여 다음 전원 인가 시 자동 실행되도록 한다.

---

## 10. Monitor Program

최종 목표는 단순히 키를 누르면 같은 문자를 LCD에 보여주는 것에서 끝나지 않는다.

CPU에서 직접 동작하는 작은 **Monitor Program**을 만드는 것을 목표로 한다.

Monitor는 일반적인 현대 OS보다 훨씬 작지만 사용자가 컴퓨터를 직접 제어할 수 있는 기본 시스템 프로그램이다.

목표 화면 예시:

```text
MAKECOMPUTER/8
SYSTEM READY

> HELP
MEM SET RUN CLS

> MEM 20
05

> SET 20 42
OK

> RUN 00
```

장기적으로 고려하는 Monitor 명령은 다음과 같다.

```text
HELP
→ 사용할 수 있는 명령 표시

MEM addr
→ 특정 RAM 주소 값 확인

SET addr value
→ 특정 RAM 주소 값 변경

DUMP start end
→ RAM 범위 출력

RUN addr
→ 지정 주소부터 프로그램 실행

CLS
→ LCD 화면 지우기

REG
→ CPU 상태 확인

LOAD
→ 키보드로 프로그램 데이터 입력
```

모든 명령을 최초 버전부터 구현할 필요는 없다.

초기 Monitor는 다음 기능부터 시작한다.

```text
1. LCD 초기화
2. 부팅 메시지 출력
3. 키보드 입력 수신
4. 입력 문자 LCD Echo
5. 간단한 명령 판별
6. RAM 읽기/쓰기
7. 프로그램 실행
```

---

## 11. Monitor를 위한 최소 명령어 확장

현재 기본 명령어만으로는 입력된 문자가 특정 문자와 같은지 판단한 뒤 서로 다른 프로그램 경로로 이동하는 작업이 어렵다.

따라서 **CPU의 기본 데이터 경로를 완전히 다시 설계하지 않는 범위에서**, Monitor 구현에 필요한 최소한의 명령어 확장을 검토한다.

우선 후보는 다음과 같다.

```text
LDI value
→ 즉시값을 A Register에 저장

CMP addr / CMP 계열
→ 값 비교

JZ addr
→ 결과가 0이면 Jump

JNZ addr
→ 결과가 0이 아니면 Jump
```

특히 조건 분기 명령은 다음과 같은 터미널 명령 해석에 필요하다.

```text
키 입력
   ↓
'M'과 비교
   ↓
같음? ── YES ──► MEM 처리
   │
   NO
   ↓
'S'와 비교
   ↓
같음? ── YES ──► SET 처리
```

기본 원칙은 **기존 CPU의 구조를 갈아엎는 것이 아니라, Monitor가 실제로 동작하는 데 필요한 최소한의 기능만 확장**하는 것이다.

---

## 12. 추가 예정 핵심 부품

키보드 + 텍스트 디스플레이 + 자동 부팅 확장에 사용할 핵심 추가 부품은 다음과 같다.

| 부품 | 예상 수량 | 역할 |
|---|---:|---|
| PS/2 Keyboard | 1 | 사용자 문자 입력 |
| Mini-DIN 6핀 PS/2 암소켓 | 1 | 키보드 연결 |
| 20×4 HD44780 호환 5V 병렬 LCD | 1 | 텍스트 출력 |
| 74HC164 | 2 | PS/2 직렬 데이터 수신 |
| 74HC14 | 1 | PS/2 신호 정리/반전 |
| 74HC74 | 2 | KEY_READY, 키 상태, Boot 상태 등 저장 |
| AT28C64B | 2 | ASCII 변환 ROM + Boot ROM |
| 4.7kΩ 저항 | 필요 수량 | PS/2 CLOCK/DATA Pull-up |
| EEPROM Programmer | 1 | EEPROM 데이터 기록 |

기존에 확보한 74HC161, 74HC273, 74HC245, 74HC138, 74HC139, 74HC08, 74HC32, 74HC04 등의 예비 부품도 확장 회로에서 최대한 재사용한다.

---

## 13. Arduino를 사용하지 않는 이유

이 프로젝트의 목적은 단순히 완성된 기능을 빠르게 구현하는 것이 아니라,

> **컴퓨터의 동작을 가능한 한 논리회로 수준에서 직접 구성하고 이해하는 것**

이다.

따라서 Arduino가 다음 기능을 대신 처리하도록 하지 않는다.

```text
PS/2 프로토콜 해석
ASCII 변환
Monitor 실행
LCD 문자 처리 로직
CPU 연산
```

필요한 변환과 상태 제어는 TTL IC, EEPROM, 직접 제작한 CPU를 중심으로 구현한다.

EEPROM Programmer는 EEPROM에 데이터를 기록하기 위한 제작 도구일 뿐 완성된 컴퓨터가 동작할 때는 연결하지 않는다.

---

## 14. 최종 사용 모습

완성된 컴퓨터는 전원을 연결하면 별도의 PC나 Arduino 없이 스스로 부팅한다.

```text
[POWER ON]
      ↓
[BOOT ROM]
      ↓
[Monitor Firmware]
      ↓
┌────────────────────┐
│ MAKECOMPUTER/8     │
│ SYSTEM READY       │
│                    │
│ > _                │
└────────────────────┘
      ▲
      │
 PS/2 Keyboard
```

사용자는 실제 키보드를 사용해 명령을 입력한다.

```text
> MEM 20
05

> SET 20 10
OK

> RUN 00
```

CPU는 직접 입력을 해석하고 RAM이나 주변장치를 제어한 뒤 결과를 텍스트 LCD에 출력한다.

---

## 15. 최종 성공 기준

프로젝트의 최종 성공 기준은 다음과 같다.

1. 기본 8비트 CPU가 독립적으로 정상 작동한다.
2. 160 byte RAM 전체를 정상적으로 읽고 쓸 수 있다.
3. PS/2 키보드의 키 입력을 외부 MCU 없이 받을 수 있다.
4. 키 입력을 CPU가 사용할 수 있는 8비트 문자 데이터로 변환할 수 있다.
5. CPU가 20×4 LCD에 원하는 문자와 문자열을 출력할 수 있다.
6. 전원을 켜면 Boot ROM의 Firmware가 자동 실행된다.
7. 사용자가 키보드로 입력한 내용을 LCD에서 확인할 수 있다.
8. Monitor Program이 최소한 RAM 확인/수정 및 프로그램 실행 기능을 제공한다.
9. 완성된 시스템은 Arduino나 Raspberry Pi 없이 독립적으로 동작한다.

---

# 최종 형태

```text
┌──────────────────────────────────────────────┐
│             MAKECOMPUTER/8                   │
│                                              │
│  직접 만든 8bit CPU                         │
│  + 160 byte RAM                              │
│  + Boot ROM                                  │
│  + Monitor Firmware                         │
│  + PS/2 Keyboard                             │
│  + Text Display                              │
│  + Memory-Mapped I/O                         │
│                                              │
│       외부 MCU 없이 독립적으로 동작          │
└──────────────────────────────────────────────┘
```

이 프로젝트의 최종 목표는 단순한 CPU 동작 시연기가 아니라,

> **전원을 켜면 스스로 부팅하고, 키보드로 명령을 입력하며, 텍스트 화면으로 결과를 확인할 수 있는 실제 사용 가능한 8비트 브레드보드 컴퓨터**

를 완성하는 것이다.
