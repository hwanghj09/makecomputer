# 최종 목표 — 키보드와 텍스트 터미널을 갖춘 8비트 브레드보드 컴퓨터

> 이 문서는 `makecomputer` 프로젝트가 최종적으로 어떤 형태의 컴퓨터를 목표로 하는지와, 그 목표를 위해 추가로 필요한 부품을 정리한 문서다.

핵심 목표는 **현재 제작 중인 8비트 브레드보드 CPU를 먼저 완성한 뒤**, CPU 본체에 **PS/2 키보드, 텍스트 디스플레이, 자동 부팅 ROM, Monitor 프로그램**을 주변장치 형태로 추가하여 실제로 사용자가 명령을 입력하고 결과를 확인할 수 있는 독립형 컴퓨터를 만드는 것이다.

---

## 1. 최종 한 줄 목표

**Arduino나 Raspberry Pi가 CPU 역할을 대신하지 않고, TTL 논리회로로 직접 만든 8비트 CPU가 PS/2 키보드 입력을 받아 텍스트 화면에 결과를 출력하며, 전원을 켜면 ROM의 Monitor 프로그램이 자동으로 실행되는 독립형 브레드보드 컴퓨터를 제작한다.**

---

## 2. 제작 순서

```text
1. 기존 8비트 CPU 완성
        ↓
2. CPU 단독 동작 검증
        ↓
3. Memory-Mapped I/O 확장
        ↓
4. 20×4 Text LCD 연결
        ↓
5. PS/2 Keyboard Interface 연결
        ↓
6. Boot ROM 연결
        ↓
7. Monitor에 필요한 최소 명령어 확장
        ↓
8. Monitor Firmware 작성
        ↓
9. 독립형 터미널 컴퓨터 완성
```

기존 컴퓨터를 버리고 새로 만드는 것이 아니라,

> **기존 CPU를 먼저 정상적으로 완성한 다음 주변장치를 하나씩 붙인다.**

---

## 3. 최종 전체 구조

```text
                         ┌─────────────────┐
                         │   PS/2 Keyboard │
                         └────────┬────────┘
                                  │ Clock / Data
                                  ▼
                    ┌────────────────────────┐
                    │ Keyboard Interface     │
                    │ 74HC14                 │
                    │ 74HC164 ×2             │
                    │ 74HC161 Counter        │
                    │ 74HC74 State/Ready     │
                    │ 74HC273 Scan Register  │
                    │ AT28C64B ASCII ROM     │
                    └───────────┬────────────┘
                                │ 8bit ASCII
                                ▼
============================ DATA BUS ============================
             │                    │                    │
             ▼                    ▼                    ▼
      ┌─────────────┐       ┌─────────────┐       ┌──────────────┐
      │ 8bit CPU    │       │ Boot ROM    │       │ 20×4 LCD     │
      │ 직접 제작   │       │ AT28C64B    │       │ HD44780      │
      └──────┬──────┘       └─────────────┘       └──────────────┘
             │
             ▼
        RAM 160 byte
```

완성 후 예상 화면:

```text
┌────────────────────┐
│ MAKECOMPUTER/8     │
│ SYSTEM READY       │
│                    │
│ > _                │
└────────────────────┘
```

---

## 4. 기존 CPU 구조

기본 CPU는 현재 설계를 우선 완성한다.

```text
Clock
PC
RAM 160 byte
A Register
IR
MAR
OUT Register
ALU
DATA BUS
Address MUX
Instruction Decoder
Microstep Decoder
Control Logic
```

RAM은 CDP1824CE 5개를 사용한다.

```text
RAM #1 :   0 ~  31
RAM #2 :  32 ~  63
RAM #3 :  64 ~  95
RAM #4 :  96 ~ 127
RAM #5 : 128 ~ 159
-------------------
총 RAM : 160 byte
```

PC와 MAR는 8비트이므로 주소 `0~255`를 표현할 수 있다.

따라서 현재 RAM이 없는 `160~255`를 주변장치용 주소 공간으로 사용한다.

---

## 5. Memory-Mapped I/O

예상 I/O 주소는 다음과 같이 잡는다.

```text
0   ~ 159 : RAM

160       : KEY_STATUS
161       : KEY_DATA

162       : LCD_DATA
163       : LCD_COMMAND

164       : BOOT_CONTROL

165 ~ 255 : 향후 확장 영역
```

예를 들어 키보드 문자를 읽는 것은 개념적으로 다음과 같다.

```text
LDA 161
```

LCD에 문자를 쓰는 것은 다음처럼 사용할 수 있다.

```text
A = 출력할 문자
STA 162
```

CPU 입장에서는 메모리를 읽고 쓰는 것과 유사하지만, 주소 디코더가 RAM 대신 Keyboard/LCD/Boot 회로를 선택한다.

---

## 6. PS/2 키보드 입력

USB Keyboard가 아니라 **PS/2 Keyboard**를 사용한다.

주요 신호는 다음과 같다.

```text
CLOCK
DATA
+5V
GND
```

키보드는 ASCII를 직접 보내지 않고 Scan Code를 직렬 전송한다.

```text
PS/2 Keyboard
      │
      ▼
74HC14
신호 정리 / 필요 시 Clock 반전
      │
      ▼
74HC164 ×2
직렬 비트 수신
      │
      ├────► 74HC161
      │      수신 비트 수 카운트
      ▼
74HC273
Scan Code 저장
      │
      ▼
AT28C64B #1
Scan Code → ASCII Lookup
      │
      ▼
74HC245
      │
      ▼
CPU DATA BUS
```

예:

```text
A 키 입력
   ↓
PS/2 Scan Code
   ↓
ASCII Lookup ROM
   ↓
01000001
   ↓
'A'
```

Arduino를 키보드 해석용 보조 프로세서로 사용하지 않는 것이 기본 원칙이다.

---

## 7. 텍스트 디스플레이

출력 장치는 **20×4 HD44780 호환 5V 병렬 문자 LCD**를 사용한다.

I²C 변환 보드에 의존하지 않고 병렬 데이터/제어선을 직접 연결하는 것을 우선한다.

LCD 자체가 문자 폰트를 가지고 있기 때문에 CPU가 픽셀을 직접 그릴 필요가 없다.

```text
01000001 → A
01000010 → B
00110001 → 1
```

CPU는 문자 코드와 LCD 제어 명령을 전달한다.

---

## 8. 자동 부팅

기존 CDP1824CE RAM은 휘발성이므로 전원을 끄면 프로그램이 사라진다.

따라서 별도의 **AT28C64B EEPROM #2를 Boot ROM**으로 사용한다.

```text
POWER ON
   ↓
RESET
   ↓
BOOT_MODE = 1
   ↓
Boot ROM 선택
   ↓
Monitor Firmware 실행
```

부팅 중에는 낮은 주소의 READ에 RAM 대신 Boot ROM이 응답하는 **Boot ROM Overlay** 방식을 목표로 한다.

```text
BOOT_MODE = 1
READ 낮은 주소 → Boot ROM

BOOT_MODE = 0
READ/WRITE → 정상 RAM
```

`BOOT_MODE` 상태는 Flip-Flop으로 기억하도록 설계한다.

---

## 9. AT28C64B 두 개의 역할

### AT28C64B #1 — Keyboard Translation ROM

```text
PS/2 Scan Code
      ↓
AT28C64B #1
      ↓
ASCII
```

### AT28C64B #2 — Boot ROM

```text
POWER ON
   ↓
AT28C64B #2
   ↓
Monitor Firmware
```

EEPROM은 전원을 꺼도 데이터가 유지된다.

---

## 10. Monitor Program

최종 목표는 문자 Echo에서 끝나지 않고 CPU에서 직접 동작하는 작은 **Monitor Program**을 만드는 것이다.

예상 사용 형태:

```text
MAKECOMPUTER/8
SYSTEM READY

> HELP
MEM SET RUN CLS

> MEM 20
05

> SET 20 42
OK

> RUN 00
```

장기적인 명령 후보:

```text
HELP
→ 명령 목록 표시

MEM addr
→ RAM 한 주소 읽기

SET addr value
→ RAM 값 수정

DUMP start end
→ RAM 범위 표시

RUN addr
→ 해당 주소부터 프로그램 실행

CLS
→ LCD 화면 지우기

REG
→ CPU 상태 표시

LOAD
→ 키보드로 프로그램 데이터 입력
```

초기 버전은 다음 기능부터 구현한다.

```text
1. LCD 초기화
2. 부팅 메시지 출력
3. 키보드 문자 수신
4. LCD Echo
5. 명령 판별
6. RAM 읽기/쓰기
7. 프로그램 실행
```

---

## 11. Monitor를 위한 최소 명령어 확장

현재 기본 명령은 다음과 같다.

```text
NOP
LDA addr
STA addr
ADD addr
SUB addr
OUT
JMP addr
HLT
```

이 상태에는 조건 분기가 없으므로 명령형 Monitor를 구현하려면 최소한 다음 기능을 추가하는 것을 목표로 한다.

```text
LDI value
→ 다음 바이트의 값을 A에 바로 저장

CMP addr 또는 비교 기능
→ A와 다른 값을 비교

JZ addr
→ 비교/연산 결과가 0이면 Jump

JNZ addr
→ 결과가 0이 아니면 Jump
```

조건 분기의 핵심은 Zero Flag다.

```text
ALU 결과 00000000
       ↓
Zero Detect
       ↓
Z = 1
       ↓
JZ 실행 가능
```

기본 원칙은 CPU 전체를 다시 설계하는 것이 아니라 **Monitor 동작에 필요한 최소 기능만 추가**하는 것이다.

---

# 12. 확장에 필요한 부품

## 12-1. 새로 구매해야 하는 핵심 부품

아래 수량은 **키보드 + 텍스트 LCD + 자동 부팅 + Monitor 최종 목표**를 기준으로 한다.

| 부품 | 권장 규격 | 구매 수량 | 역할 |
|---|---|---:|---|
| **PS/2 Keyboard** | 표준 PS/2 키보드 | **1** | 실제 문자 입력 |
| **PS/2 Mini-DIN 6핀 암소켓** | PCB/브레드보드 연결 가능한 타입 | **1~2** | 키보드 연결 |
| **20×4 Character LCD** | **HD44780 호환, 5V, 16핀 병렬 방식** | **1** | 텍스트 터미널 화면 |
| **74HC164N** | PDIP-14 | **2** | PS/2 직렬 프레임을 병렬 비트로 수신 |
| **74HC14N** | PDIP-14 Schmitt Trigger Inverter | **1** | PS/2 Clock/Data 신호 정리 및 반전 |
| **74HC74N** | PDIP-14 Dual D Flip-Flop | **2** | KEY_READY, Release/Shift 상태, BOOT_MODE 등의 1비트 상태 기억 |
| **AT28C64B** | 5V Parallel EEPROM, DIP-28 권장 | **2** | #1 Scan Code→ASCII, #2 Boot/Monitor ROM |
| **4.7kΩ 저항** | 1/4W 일반 저항 | **10개 이상** | PS/2 Clock/Data Pull-up 및 예비 |
| **EEPROM Programmer** | AT28C64B 쓰기 지원 제품 | **1** | ASCII Table / Monitor Firmware 기록 |
| **2.54mm 1×16 핀헤더** | Male Header | **1~2** | LCD에 핀이 없는 경우 사용 |
| **830홀 브레드보드** | 기존과 같은 규격 권장 | **2** | Keyboard/Boot/LCD 확장 회로 |
| **M/M 점퍼선** | 브레드보드용 | **100개 이상 권장** | 확장 배선 |

### 구매 시 특히 주의할 것

```text
LCD
→ I²C 전용 제품보다 HD44780 16핀 병렬형 우선

AT28C64B
→ 병렬 EEPROM, 5V, DIP 패키지 우선

74HC 계열
→ 브레드보드에 꽂을 수 있는 PDIP/N 패키지 우선

PS/2 Socket
→ Mini-DIN 6핀의 핀 배열을 반드시 확인
```

---

## 12-2. 기존 보유 부품에서 확장에 재사용할 부품

현재 기본 CPU를 만든 뒤 남는 예비 수량을 우선 활용한다.

| 부품 | 현재 CPU 기본 사용 | 현재 확보 | 기본 CPU 완성 후 예비 | 확장에서 예상 역할 |
|---|---:|---:|---:|---|
| **74HC161** | 3 | 5 | **2** | PS/2 수신 비트 카운터 등 |
| **74HC273** | 4 | 5 | **1** | Scan Code 8비트 Register |
| **74HC245** | 3 | 5 | **2** | Keyboard/ROM과 DATA BUS 연결 |
| **74HC157** | 4 | 6 | **2** | Boot/주소/Data 경로 선택에 활용 가능 |
| **74HC138** | 2 | 3 | **1** | I/O 영역 또는 확장 주소 Decode |
| **74HC139** | 1 | 3 | **2** | KEY/LCD/BOOT 세부 주소 Decode, 확장 명령 Decode |
| **74HC08** | 3 | 5 | **2** | 제어 신호 AND 조합 |
| **74HC32** | 3 | 5 | **2** | Zero Detect/I/O 제어 OR 조합 |
| **74HC04** | 2 | 4 | **2** | 신호 반전 / Zero Detect 마무리 |
| **10kΩ 가변저항** | 일부 사용 | 3 확보 | 여유 있음 | LCD Contrast 조정 |
| **0.1µF Ceramic Capacitor** | IC마다 사용 | 60 확보 | 확장용 사용 | 새 IC별 VCC-GND Decoupling |
| **330Ω 저항** | LED용 | 100 확보 | 여유 있음 | LCD Backlight/LED 필요 시 |
| **5V 2A Adapter** | 1 | 1 | 기존 사용 | 전체 시스템 5V 전원 |

> 실제 확장 배선이 확정되면 여분 게이트의 정확한 소비량을 다시 계산한다. 특히 Zero Flag, Boot Overlay, I/O Decode를 동시에 구성하면서 기존 예비 게이트가 부족하면 74HC08/74HC32/74HC04/74HC139 등을 1~2개 추가 구매할 수 있다.

---

## 12-3. 추가 구매를 권장하는 예비 논리 IC

최종 회로 설계 중 게이트 수 부족으로 작업이 멈추지 않도록 다음 정도를 예비로 더 확보하는 것을 권장한다.

| 부품 | 추가 권장 수량 | 이유 |
|---|---:|---|
| **74HC08N** | 1 | Boot/I/O/조건분기 제어 AND 여유 확보 |
| **74HC32N** | 1 | Zero Detect 및 제어 OR 여유 확보 |
| **74HC04N** | 1 | Active-Low/Zero Flag 반전 여유 |
| **74HC139N** | 1 | I/O 및 확장 명령 Decode 여유 |
| **74HC245N** | 1 | ROM/주변장치 Bus 연결 여유 |

이 부분은 절대 필수라기보다 **최종 Monitor 회로까지 만들 때 부족할 가능성을 줄이기 위한 예비 구매**다.

---

## 12-4. EEPROM Programmer는 완성품 구성요소가 아니다

```text
PC
 ↓
EEPROM Programmer
 ↓
AT28C64B에 데이터 기록
 ↓
AT28C64B를 브레드보드에 장착
```

EEPROM Programmer는 제작 과정에서만 사용한다.

완성된 컴퓨터는 Programmer나 PC 연결 없이 독립 동작한다.

---

## 13. 새 부품 역할 요약

```text
PS/2 Keyboard
= 사용자가 문자 입력

Mini-DIN 6핀
= 키보드와 회로 연결

74HC14
= 외부 PS/2 신호 정리

74HC164 ×2
= 한 비트씩 들어오는 PS/2 데이터를 모음

74HC161
= 몇 비트를 받았는지 셈

74HC74
= KEY_READY / 키 상태 / BOOT_MODE 같은 상태 기억

74HC273
= 완성된 Scan Code 저장

AT28C64B #1
= Scan Code를 ASCII 문자로 번역

AT28C64B #2
= 전원을 켰을 때 실행될 Monitor Firmware 저장

74HC245
= 주변장치가 필요할 때만 CPU DATA BUS를 구동

HD44780 20×4 LCD
= CPU가 보내는 문자 표시
```

---

## 14. Arduino를 사용하지 않는 이유

이 프로젝트의 목적은 단순히 기능을 빠르게 구현하는 것이 아니라,

> **컴퓨터의 동작을 가능한 한 논리회로 수준에서 직접 구성하고 이해하는 것**

이다.

따라서 Arduino가 다음 기능을 대신하지 않는다.

```text
PS/2 프로토콜 처리
ASCII 변환
Boot/Monitor 실행
CPU 연산
터미널 명령 해석
```

EEPROM Programmer는 EEPROM에 데이터를 기록하기 위한 제작 도구일 뿐 완성된 시스템의 프로세서가 아니다.

---

## 15. 최종 사용 모습

```text
[POWER ON]
      ↓
[RESET]
      ↓
[BOOT ROM]
      ↓
[Monitor Firmware]
      ↓
┌────────────────────┐
│ MAKECOMPUTER/8     │
│ SYSTEM READY       │
│                    │
│ > _                │
└────────────────────┘
      ▲
      │
 PS/2 Keyboard
```

사용자는 키보드로 명령을 입력한다.

```text
> MEM 20
05

> SET 20 10
OK

> RUN 00
```

CPU가 직접 입력을 해석하고 RAM/주변장치를 제어한 뒤 결과를 LCD에 표시한다.

---

## 16. 최종 성공 기준

1. 기본 8비트 CPU가 독립적으로 정상 작동한다.
2. 160 byte RAM 전체를 정상적으로 읽고 쓸 수 있다.
3. PS/2 키보드 입력을 외부 MCU 없이 받을 수 있다.
4. Scan Code를 CPU가 사용하는 문자 데이터로 변환할 수 있다.
5. CPU가 20×4 LCD에 문자와 문자열을 출력할 수 있다.
6. 전원 ON 시 Boot ROM의 Firmware가 자동으로 실행된다.
7. 키보드로 입력한 내용을 LCD에서 확인할 수 있다.
8. 조건분기를 이용해 Monitor가 사용자의 명령을 판별할 수 있다.
9. Monitor가 최소한 RAM 확인/수정 및 프로그램 실행 기능을 제공한다.
10. Arduino나 Raspberry Pi 없이 독립적으로 동작한다.

---

# 최종 형태

```text
┌──────────────────────────────────────────────┐
│             MAKECOMPUTER/8                   │
│                                              │
│  직접 만든 8bit CPU                         │
│  + 160 byte RAM                              │
│  + Boot ROM                                  │
│  + Monitor Firmware                         │
│  + PS/2 Keyboard                             │
│  + 20×4 Text Display                         │
│  + Memory-Mapped I/O                         │
│                                              │
│       외부 MCU 없이 독립적으로 동작          │
└──────────────────────────────────────────────┘
```

이 프로젝트의 최종 목표는 단순한 CPU 동작 시연기가 아니라,

> **전원을 켜면 스스로 부팅하고, 실제 키보드로 명령을 입력하며, 텍스트 화면으로 결과를 확인할 수 있는 사용 가능한 8비트 브레드보드 컴퓨터**

를 완성하는 것이다.
