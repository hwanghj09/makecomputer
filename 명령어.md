# 8비트 컴퓨터 명령어 구조

> 이 문서는 우리가 만드는 8비트 컴퓨터에서 **명령어가 어떻게 저장되고, 해석되고, 실행되는지**를 설명한다.
>
> 컴퓨터 용어가 처음 나올 때는 바로 뜻을 설명한다.

---

## 목차

1. [먼저 알아야 할 용어](#1-먼저-알아야-할-용어)
2. [명령어의 기본 구조](#2-명령어의-기본-구조)
3. [Opcode는 3비트](#3-opcode는-3비트)
4. [명령어 목록](#4-명령어-목록)
5. [1바이트 명령어와 2바이트 명령어](#5-1바이트-명령어와-2바이트-명령어)
6. [PC가 실제로 세는 것](#6-pc가-실제로-세는-것)
7. [명령어를 해석하는 과정](#7-명령어를-해석하는-과정)
8. [마이크로스텝](#8-마이크로스텝)
9. [모든 명령어가 공통으로 하는 일](#9-모든-명령어가-공통으로-하는-일)
10. [각 명령어의 실행 과정](#10-각-명령어의-실행-과정)
11. [제어 신호](#11-제어-신호)
12. [명령어와 마이크로스텝 전체 표](#12-명령어와-마이크로스텝-전체-표)
13. [실제 프로그램 예시](#13-실제-프로그램-예시)
14. [명령어를 만들 때 지켜야 하는 규칙](#14-명령어를-만들-때-지켜야-하는-규칙)

---

# 1. 먼저 알아야 할 용어

명령어를 설명하기 전에 몇 가지 단어만 알고 가자.

## Bit

**Bit(비트)**는 컴퓨터가 다루는 가장 작은 정보 단위다.

한 비트에는 둘 중 하나만 들어갈 수 있다.

```text
0
또는
1
```

---

## Byte

**Byte(바이트)**는 8개의 비트를 묶은 단위다.

```text
1 Byte = 8 Bit
```

예를 들어

```text
10110010
```

은 8비트이므로 1바이트다.

우리 컴퓨터는 한 번에 **8비트 데이터**를 다루기 때문에 명령어도 기본적으로 8비트 단위로 RAM에 저장한다.

---

## Address

**Address(주소)**는 RAM의 몇 번째 저장공간을 사용할지 나타내는 번호다.

예를 들어

```text
RAM[20]
```

은

> RAM의 20번 주소

라는 뜻이다.

우리 컴퓨터는 CDP1824CE RAM을 3개 사용하므로 실제 사용 가능한 RAM 주소는

```text
0 ~ 95
```

이다.

---

## Instruction

**Instruction(명령어)**은 CPU에게 무엇을 해야 하는지 알려주는 값이다.

예를 들어

```text
LDA 20
```

은

> RAM 20번 주소의 값을 A Register에 가져와라

라는 의미로 사용할 수 있다.

---

## Opcode

**Opcode(오퍼레이션 코드)**는 명령어에서

> 무엇을 할 것인가?

를 나타내는 부분이다.

예를 들어

```text
001 = LDA
011 = ADD
101 = OUT
```

처럼 정한다.

---

## Operand

**Operand(피연산자)**는 명령어가 사용할 추가 정보다.

우리 컴퓨터에서는 주로 **RAM 주소**가 Operand가 된다.

예를 들어

```text
LDA 20
```

에서

```text
LDA
```

는 Opcode이고,

```text
20
```

은 Operand다.

그림으로 보면

```text
LDA 20
│   │
│   └── Operand : 어디를 사용할 것인가?
│
└────── Opcode  : 무엇을 할 것인가?
```

---

## Register

**Register(레지스터)**는 CPU 내부에서 값을 잠깐 기억하는 작은 저장장치다.

우리 컴퓨터에서는 주로 다음 레지스터를 사용한다.

| 이름 | 뜻 | 역할 |
|---|---|---|
| A | A Register | 계산에 사용할 값 저장 |
| IR | Instruction Register | 현재 명령어 저장 |
| MAR | Memory Address Register | 사용할 RAM 주소 저장 |
| OUT | Output Register | 출력할 값 저장 |

---

# 2. 명령어의 기본 구조

우리 컴퓨터의 기본 데이터 크기는 8비트다.

그래서 RAM 한 칸에는 항상 이런 식으로 8비트가 들어간다.

```text
Bit 번호

 7 6 5 4 3 2 1 0
┌─┬─┬─┬─┬─┬─┬─┬─┐
│ │ │ │ │ │ │ │ │
└─┴─┴─┴─┴─┴─┴─┴─┘
```

명령어를 저장하는 바이트는 다음처럼 사용한다.

```text
 7 6 5 | 4 3 2 1 0
-------+-----------
Opcode | Reserved
```

**Reserved(예약 영역)**는 지금은 사용하지 않고 나중에 기능을 확장할 때 사용할 수 있도록 남겨둔 비트다.

현재는 이 부분을 모두 `0`으로 둔다.

따라서 명령어 바이트의 구조는

```text
[ Opcode 3bit ][ 00000 ]
```

이다.

예를 들어 Opcode가

```text
011
```

이면 실제 8비트 명령어는

```text
01100000
```

이 된다.

---

# 3. Opcode는 3비트

Opcode는 **3비트**를 사용한다.

3비트로 만들 수 있는 조합은

```text
000
001
010
011
100
101
110
111
```

총 8개다.

즉

```text
2³ = 8
```

이므로 최대 8개의 기본 명령어를 구분할 수 있다.

---

## 명령어 해석에 사용하는 비트

Instruction Register, 줄여서 **IR**의 상위 3비트를 사용한다.

```text
IR7 IR6 IR5 | IR4 IR3 IR2 IR1 IR0
------------+---------------------
   Opcode   |      Reserved
```

따라서 명령어 디코더는

```text
IR7
IR6
IR5
```

세 비트만 보면 현재 어떤 명령어인지 알 수 있다.

---

# 4. 명령어 목록

현재 명령어는 다음 8개로 구성한다.

| Opcode | 실제 바이트 | 명령어 | 의미 | 길이 |
|---|---|---|---|---:|
| `000` | `00000000` | `NOP` | 아무 동작도 하지 않음 | 1 Byte |
| `001` | `00100000` | `LDA addr` | RAM 값을 A에 저장 | 2 Byte |
| `010` | `01000000` | `STA addr` | A 값을 RAM에 저장 | 2 Byte |
| `011` | `01100000` | `ADD addr` | RAM 값을 A에 더함 | 2 Byte |
| `100` | `10000000` | `SUB addr` | RAM 값을 A에서 뺌 | 2 Byte |
| `101` | `10100000` | `OUT` | A 값을 출력 | 1 Byte |
| `110` | `11000000` | `JMP addr` | 지정한 주소로 이동 | 2 Byte |
| `111` | `11100000` | `HLT` | 컴퓨터 실행 정지 | 1 Byte |

`addr`은 **Address(주소)**의 줄임말이다.

예를 들어

```text
LDA 20
```

은

> 주소 20의 데이터를 A Register로 가져와라

라는 뜻이다.

---

# 5. 1바이트 명령어와 2바이트 명령어

모든 명령어의 크기가 같은 것은 아니다.

## 1바이트 명령어

추가 주소가 필요하지 않은 명령어다.

```text
NOP
OUT
HLT
```

예를 들어 `OUT`은

```text
10100000
```

한 바이트만 있으면 된다.

RAM에서는 한 칸만 차지한다.

```text
주소 10 : 10100000   ← OUT
```

---

## 2바이트 명령어

RAM 주소가 필요한 명령어다.

```text
LDA
STA
ADD
SUB
JMP
```

예를 들어

```text
LDA 20
```

은 RAM에 다음처럼 저장한다.

```text
주소 0 : 00100000   ← LDA
주소 1 : 00010100   ← 주소 20
```

첫 번째 바이트:

```text
00100000
```

은

> 어떤 동작인가?

를 알려준다.

두 번째 바이트:

```text
00010100
```

은

> 어느 RAM 주소를 사용할 것인가?

를 알려준다.

---

## 왜 주소를 따로 저장할까?

RAM은 총 96바이트이므로 주소를

```text
0 ~ 95
```

까지 표현해야 한다.

이 주소를 표현하려면 최소 7비트가 필요하다.

그런데 Opcode도 3비트가 필요하다.

```text
Opcode 3bit
+
Address 7bit
=
10bit
```

한 바이트는 8비트밖에 없으므로 한 바이트 안에 둘 다 넣을 수 없다.

그래서

```text
[명령어 바이트]
[주소 바이트]
```

두 칸으로 나눈다.

---

# 6. PC가 실제로 세는 것

**PC(Program Counter)**는 다음에 읽을 RAM 주소를 기억하는 장치다.

중요한 점은 PC가

> 몇 번째 명령어인가?

를 직접 세는 것이 아니라

> **다음에 읽을 몇 번째 바이트인가?**

를 센다는 것이다.

---

## 예시

RAM에 다음 프로그램이 있다고 하자.

```text
주소  데이터       의미
-----------------------
0     00100000     LDA
1     00010100     20

2     01100000     ADD
3     00010101     21

4     10100000     OUT

5     11100000     HLT
```

PC의 움직임은 다음과 같다.

```text
PC = 0
↓
LDA 읽기

PC = 1
↓
주소 20 읽기

PC = 2
↓
ADD 읽기

PC = 3
↓
주소 21 읽기

PC = 4
↓
OUT 읽기

PC = 5
↓
HLT 읽기
```

따라서

```text
2바이트 명령어
→ PC가 2칸 진행

1바이트 명령어
→ PC가 1칸 진행
```

한다.

---

# 7. 명령어를 해석하는 과정

RAM에서 읽은 명령어는 먼저 **IR(Instruction Register)**에 저장한다.

IR은

> 지금 실행하고 있는 명령어를 잠깐 기억하는 레지스터

다.

전체 흐름은 다음과 같다.

```text
RAM
 │
 │ 명령어 8bit
 ▼
Data BUS
 │
 ▼
IR
 │
 │ IR7, IR6, IR5
 ▼
명령어 디코더
 │
 ├── NOP
 ├── LDA
 ├── STA
 ├── ADD
 ├── SUB
 ├── OUT
 ├── JMP
 └── HLT
```

---

## 74HC139의 역할

우리 컴퓨터에서는 `74HC139`를 **Instruction Decoder(명령어 디코더)**로 사용한다.

**Decoder(디코더)**란 이진수 값을 보고 여러 출력 중 하나를 선택하는 회로다.

74HC139 안에는

```text
2 → 4 Decoder
```

가 두 개 들어 있다.

두 부분을 같이 사용해서 3비트 Opcode 8개를 구분한다.

개념적으로는 다음과 같다.

```text
             IR7
              │
        ┌─────┴─────┐
        │           │
   Decoder A    Decoder B
      활성         활성
        │           │
      IR6 IR5     IR6 IR5
        │           │
   ┌────┼────┐ ┌────┼────┐
   ▼    ▼    ▼ ▼    ▼    ▼
 OP0  OP1 ...    ...      OP7
```

실제 74HC139의 출력은 **Active-Low**다.

**Active-Low**는

> 신호가 `0`일 때 선택되거나 작동한다

는 뜻이다.

따라서 실제 회로에서는 `/LDA`, `/ADD`처럼 LOW일 때 선택되는 신호가 나오게 된다.

---

# 8. 마이크로스텝

명령어 하나도 내부에서는 여러 작은 단계로 나누어 실행한다.

이 작은 단계를 **Microstep(마이크로스텝)**이라고 한다.

예를 들어 명령어 하나를

```text
T0
T1
T2
T3
T4
T5
```

처럼 나눌 수 있다.

`T`는 여기서 각 실행 단계를 구분하기 위한 이름이다.

---

## Microstep Counter

세 번째 `74HC161`은 **Microstep Counter**로 사용한다.

Counter(카운터)는 숫자를 하나씩 세는 장치다.

```text
000
↓
001
↓
010
↓
011
↓
100
↓
101
↓
...
```

이 중 하위 3비트를 사용하면 8개의 상태를 만들 수 있다.

```text
000 → T0
001 → T1
010 → T2
011 → T3
100 → T4
101 → T5
110 → T6
111 → T7
```

---

## 74HC138의 역할

`74HC138`은 **3-to-8 Decoder**다.

즉 3비트 입력을 받아 8개의 단계 중 하나를 선택한다.

```text
Microstep Counter
       │
    Q2 Q1 Q0
       │
       ▼
   74HC138
       │
 ┌─────┼──────────────────────────┐
 ▼     ▼     ▼     ▼     ▼     ▼
T0    T1    T2    T3    T4    T5 ...
```

현재 설계에서는 가장 긴 명령어도 **T0~T5**, 총 6단계 안에 실행하도록 한다.

따라서

```text
T6
T7
```

은 나중에 더 복잡한 동작을 추가할 때 사용할 수 있다.

74HC138의 출력 역시 **Active-Low**다.

즉 실제 핀에서는 선택된 단계가 LOW가 된다.

---

# 9. 모든 명령어가 공통으로 하는 일

CPU는 먼저

> 지금 실행해야 할 명령어가 무엇인지

알아야 한다.

그래서 모든 명령어는 처음에 **Fetch(페치)** 과정을 거친다.

**Fetch**는

> RAM에서 다음 명령어를 가져오는 과정

이라는 뜻이다.

---

## T0 - 명령어가 있는 주소 선택

Address MUX가 PC를 선택한다.

**MUX(Multiplexer)**는 여러 입력 중 하나를 선택하는 전자 스위치다.

```text
PC ───────────┐
              │
              ▼
          Address MUX
              │
MAR ──────────┘
              │
              ▼
         Address BUS
              │
              ▼
             RAM
```

T0에서는

```text
Address MUX = PC
```

로 만든다.

그러면 RAM은 PC가 가리키는 주소를 선택한다.

---

## T1 - 명령어 읽기

RAM의 8비트 값을 IR에 저장한다.

```text
RAM
 │
 ▼
Data BUS
 │
 ▼
IR
```

동시에 PC를 1 증가시킨다.

```text
IR ← RAM[PC]

PC ← PC + 1
```

`←` 기호는

> 오른쪽 값을 왼쪽에 저장한다

는 뜻이다.

예를 들어

```text
A ← 5
```

는

> A에 5를 저장한다

라는 뜻이다.

---

## Fetch 이후

T1이 끝나면 IR 안에는 현재 명령어가 들어 있다.

```text
IR7 IR6 IR5
      │
      ▼
  74HC139
      │
      ▼
현재 명령어 선택
```

그 다음부터는 명령어에 따라 서로 다른 동작을 한다.

---

# 10. 각 명령어의 실행 과정

# NOP

`NOP`는 **No Operation**의 줄임말이다.

뜻은

> 아무 것도 하지 않는다

이다.

```text
T0 : PC 주소 선택
T1 : RAM → IR, PC + 1
T2 : 실행 종료
```

총 3단계다.

---

# LDA addr

`LDA`는

> 지정한 RAM 주소의 값을 A Register에 저장

하는 명령어다.

예:

```text
LDA 20
```

뜻:

```text
A ← RAM[20]
```

실행 흐름:

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1

T2 : PC 주소 선택
T3 : 다음 바이트에 들어 있는 주소를 MAR에 저장, PC + 1

T4 : RAM 주소를 MAR로 변경
T5 : RAM의 값을 A에 저장
```

그림으로 보면

```text
명령어 읽기

PC
 │
 ▼
RAM
 │
 ▼
IR


주소 읽기

PC
 │
 ▼
RAM
 │
 ▼
MAR


데이터 읽기

MAR
 │
 ▼
RAM
 │
 ▼
BUS
 │
 ▼
A
```

---

# STA addr

`STA`는

> A Register의 값을 지정한 RAM 주소에 저장

하는 명령어다.

예:

```text
STA 30
```

뜻:

```text
RAM[30] ← A
```

실행 흐름:

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1

T2 : PC 주소 선택
T3 : 다음 바이트의 주소를 MAR에 저장, PC + 1

T4 : RAM 주소를 MAR로 변경
T5 : A의 값을 RAM에 저장
```

데이터 이동은 다음과 같다.

```text
A Register
     │
     ▼
  74HC245
     │
     ▼
 Data BUS
     │
     ▼
RAM[MAR]
```

---

# ADD addr

`ADD`는

> 지정한 RAM 주소의 값을 A에 더하는 명령어

다.

예:

```text
ADD 21
```

RAM[21]에 3이 있고 A에 5가 있다면

```text
A = 5
RAM[21] = 3
```

실행 후

```text
A = 8
```

이 된다.

즉

```text
A ← A + RAM[21]
```

이다.

실행 흐름:

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1

T2 : PC 주소 선택
T3 : 다음 바이트의 주소를 MAR에 저장, PC + 1

T4 : RAM 주소를 MAR로 변경
T5 : ALU 계산 결과를 A에 저장
```

**ALU(Arithmetic Logic Unit)**는 CPU 안에서 덧셈이나 뺄셈을 하는 계산 회로다.

```text
A ───────────┐
             ▼
            ALU ───→ 결과
             ▲
             │
RAM[MAR] ────┘
```

---

# SUB addr

`SUB`는

> 지정한 RAM 값을 A에서 빼는 명령어

다.

예:

```text
SUB 21
```

A가 8이고 RAM[21]이 3이라면

```text
8 - 3 = 5
```

가 된다.

즉

```text
A ← A - RAM[21]
```

이다.

실행 단계는 ADD와 거의 같다.

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1

T2 : PC 주소 선택
T3 : 다음 바이트의 주소를 MAR에 저장, PC + 1

T4 : RAM 주소를 MAR로 변경
T5 : 뺄셈 결과를 A에 저장
```

T5에서 `SUB` 제어 신호를 켜면

`74HC86`과 가산기가 함께 동작하여 뺄셈을 수행한다.

---

# OUT

`OUT`은

> A Register의 값을 Output Register에 저장

하는 명령어다.

Output Register는 출력 값을 계속 기억하는 레지스터다.

```text
A
│
▼
BUS
│
▼
OUT Register
│
▼
LED
```

실행 흐름:

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1
T2 : A의 값을 OUT Register에 저장
```

---

# JMP addr

`JMP`는 **Jump(점프)**의 줄임말이다.

뜻은

> 프로그램 실행 위치를 다른 주소로 이동한다

이다.

예를 들어

```text
JMP 10
```

을 실행하면

```text
PC ← 10
```

이 된다.

그러면 다음 명령어는 RAM 10번 주소에서 읽는다.

실행 흐름:

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1

T2 : PC 주소 선택
T3 : 다음 바이트의 주소를 MAR에 저장, PC + 1

T4 : MAR 값을 PC에 저장
```

흐름은

```text
RAM
 │
 ▼
MAR
 │
 ▼
PC
```

다.

---

# HLT

`HLT`는 **Halt(정지)**의 줄임말이다.

뜻은

> CPU 실행을 멈춘다

이다.

실행 흐름:

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1
T2 : HALT 상태로 전환
```

HALT 상태에서는 더 이상 새로운 명령어를 실행하지 않는다.

실제 클럭 정지 회로는 전체 제어회로를 배선할 때 구현한다.

---

# 11. 제어 신호

**Control Signal(제어 신호)**은

> 각 부품에게 지금 무엇을 해야 하는지 알려주는 0 또는 1 신호

다.

예를 들어

```text
PC_COUNT
```

라는 신호가 켜지면

> PC를 1 증가시켜라

라는 의미로 사용할 수 있다.

---

## 사용할 주요 제어 신호

| 제어 신호 | 의미 |
|---|---|
| `RAM_OUT` | RAM 값을 Data BUS에 출력 |
| `RAM_WRITE` | BUS 값을 RAM에 저장 |
| `A_OUT` | A 값을 BUS에 출력 |
| `A_LOAD` | BUS 값을 A에 저장 |
| `IR_LOAD` | BUS 값을 IR에 저장 |
| `MAR_LOAD` | BUS 값을 MAR에 저장 |
| `OUT_LOAD` | BUS 값을 OUT에 저장 |
| `ALU_OUT` | ALU 결과를 BUS에 출력 |
| `PC_COUNT` | PC를 1 증가 |
| `PC_LOAD` | 새 주소를 PC에 저장 |
| `ADDR_SEL` | PC와 MAR 중 RAM 주소로 사용할 것을 선택 |
| `SUB` | ALU를 뺄셈 상태로 변경 |
| `STEP_RESET` | Microstep Counter를 T0으로 되돌림 |
| `HALT` | CPU 실행 정지 |

---

## 예: ADD의 마지막 단계

현재 상태가

```text
명령어 = ADD
Microstep = T5
```

라고 하자.

이때 필요한 제어 신호는 개념적으로

```text
ALU_OUT = ON
A_LOAD  = ON
STEP_RESET = ON
```

이다.

즉

```text
ALU 계산 결과
      │
      ▼
    BUS
      │
      ▼
A Register
```

로 값을 옮긴 후 다음 명령어를 실행하기 위해 T0으로 돌아간다.

---

# 12. 명령어와 마이크로스텝 전체 표

아래 표는 각 명령어가 몇 단계까지 사용하는지 한눈에 보여준다.

| 명령어 | T0 | T1 | T2 | T3 | T4 | T5 |
|---|---|---|---|---|---|---|
| `NOP` | 주소 준비 | IR 저장 | 종료 | - | - | - |
| `LDA` | 주소 준비 | IR 저장 | 주소 바이트 준비 | MAR 저장 | MAR 주소 선택 | RAM → A |
| `STA` | 주소 준비 | IR 저장 | 주소 바이트 준비 | MAR 저장 | MAR 주소 선택 | A → RAM |
| `ADD` | 주소 준비 | IR 저장 | 주소 바이트 준비 | MAR 저장 | MAR 주소 선택 | ALU → A |
| `SUB` | 주소 준비 | IR 저장 | 주소 바이트 준비 | MAR 저장 | MAR 주소 선택 | ALU → A |
| `OUT` | 주소 준비 | IR 저장 | A → OUT | - | - | - |
| `JMP` | 주소 준비 | IR 저장 | 주소 바이트 준비 | MAR 저장 | MAR → PC | - |
| `HLT` | 주소 준비 | IR 저장 | 정지 | - | - | - |

여기서 `-`는 그 단계까지 가지 않는다는 뜻이다.

---

## 단계 수 정리

| 명령어 | 사용하는 단계 수 |
|---|---:|
| NOP | 3 |
| LDA | 6 |
| STA | 6 |
| ADD | 6 |
| SUB | 6 |
| OUT | 3 |
| JMP | 5 |
| HLT | 3 |

가장 긴 명령어가 6단계이므로 현재의

```text
T0 ~ T7
```

8단계 구조 안에 충분히 들어간다.

---

# 13. 실제 프로그램 예시

숫자 5와 숫자 3을 더해서 출력해보자.

데이터는 다음 위치에 저장한다고 하자.

```text
RAM[20] = 5
RAM[21] = 3
```

프로그램은

```text
LDA 20
ADD 21
OUT
HLT
```

이다.

---

## 사람이 읽는 프로그램

```text
LDA 20
ADD 21
OUT
HLT
```

---

## RAM에 실제로 들어가는 값

| 주소 | 2진수 | 16진수 | 의미 |
|---:|---|---|---|
| 0 | `00100000` | `0x20` | LDA |
| 1 | `00010100` | `0x14` | 주소 20 |
| 2 | `01100000` | `0x60` | ADD |
| 3 | `00010101` | `0x15` | 주소 21 |
| 4 | `10100000` | `0xA0` | OUT |
| 5 | `11100000` | `0xE0` | HLT |
| 20 | `00000101` | `0x05` | 숫자 5 |
| 21 | `00000011` | `0x03` | 숫자 3 |

**16진수(Hexadecimal)**는 이진수를 짧게 표시하는 숫자 표기법이다.

예를 들어

```text
00100000₂ = 0x20
```

처럼 쓸 수 있다.

---

## 프로그램 실행 흐름

```text
PC = 0
 │
 ▼
LDA 20
 │
 ▼
A = 5
 │
 ▼
PC = 2
 │
 ▼
ADD 21
 │
 ▼
A = 8
 │
 ▼
PC = 4
 │
 ▼
OUT
 │
 ▼
OUT Register = 8
 │
 ▼
PC = 5
 │
 ▼
HLT
```

---

# 14. 명령어를 만들 때 지켜야 하는 규칙

## 1. RAM 주소는 0~95만 사용한다

현재 실제 RAM은 96바이트다.

따라서 유효한 주소는

```text
0 ~ 95
```

이다.

```text
96 ~ 255
```

는 PC가 숫자로 표현할 수는 있지만 현재 실제 RAM이 연결되어 있지 않다.

---

## 2. Opcode 바이트의 하위 5비트는 현재 0으로 둔다

현재 구조는

```text
Opcode | Reserved
 3bit  |  5bit
```

이다.

따라서 예를 들어 LDA는

```text
00100000
```

으로 사용한다.

현재는

```text
00110110
```

처럼 Reserved 영역에 임의의 값을 넣지 않는다.

---

## 3. 주소가 필요한 명령어 뒤에는 반드시 주소 바이트가 있어야 한다

잘못된 예:

```text
주소 0 : LDA
주소 1 : ADD
```

이렇게 되면 CPU는 주소 1의 `ADD` 값을 LDA의 주소라고 잘못 읽는다.

올바른 예:

```text
주소 0 : LDA
주소 1 : 20
주소 2 : ADD
주소 3 : 21
```

---

## 4. 프로그램과 데이터가 같은 RAM을 사용한다

우리 컴퓨터는 프로그램과 일반 데이터를 같은 RAM에 저장한다.

따라서 서로 겹치지 않게 배치해야 한다.

예를 들어

```text
0 ~ 19
→ 프로그램

20 ~ 95
→ 데이터
```

처럼 나누어 사용할 수 있다.

이 구분은 하드웨어가 자동으로 만드는 것이 아니라 프로그램을 작성하는 사람이 정한다.

---

## 5. 한 순간에 Data BUS를 출력하는 장치는 하나만 허용한다

예를 들어

```text
RAM_OUT = ON
ALU_OUT = ON
```

을 동시에 만들면 안 된다.

두 장치가 같은 BUS를 서로 다른 값으로 밀어낼 수 있기 때문이다.

따라서 원칙은

> **BUS 출력은 한 번에 하나**

이다.

---

## 6. 명령어가 끝나면 Microstep Counter를 T0으로 되돌린다

예를 들어 `OUT`은 T2에서 끝난다.

그러면

```text
T2
↓
STEP_RESET
↓
T0
```

으로 돌아가야 한다.

그 다음 T0부터 새로운 명령어를 가져온다.

---

# 전체 명령어 구조 한눈에 보기

```text
                        CLOCK
                          │
                          ▼
                  Microstep Counter
                     74HC161
                          │
                       Q2 Q1 Q0
                          │
                          ▼
                      74HC138
                      T0 ~ T7
                          │
                          │
                          ▼

RAM ──→ BUS ──→ IR ──→ IR7 IR6 IR5 ──→ 74HC139
                                         │
                                         ▼
                              현재 명령어 선택
                                         │
                                         │
              ┌──────────────────────────┴──────────────────────────┐
              │                                                     │
              ▼                                                     ▼
       현재 Microstep                                        현재 Opcode
              │                                                     │
              └──────────────────────┬──────────────────────────────┘
                                     ▼
                              Control Logic
                                     │
          ┌──────────┬──────────┬────┴────┬──────────┬───────────┐
          ▼          ▼          ▼         ▼          ▼           ▼
       PC_COUNT   RAM_OUT    A_LOAD    ALU_OUT   MAR_LOAD    OUT_LOAD
```

CPU는 결국

```text
현재 어떤 명령어인가?
+
현재 몇 번째 단계인가?
```

두 정보를 조합해서

```text
지금 어떤 부품을 움직일 것인가?
```

를 결정한다.
