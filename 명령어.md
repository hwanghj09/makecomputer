# 8비트 컴퓨터 명령어 구조

> 이 문서는 우리가 만드는 8비트 컴퓨터에서 **명령어가 어떻게 저장되고, 해석되고, 실행되는지**를 설명한다.
>
> 현재 메모리 구조는 **CDP1824CE 5개 = 160 byte, 주소 0~159**를 기준으로 한다.

---

## 목차

1. [먼저 알아야 할 용어](#1-먼저-알아야-할-용어)
2. [명령어의 기본 구조](#2-명령어의-기본-구조)
3. [Opcode는 3비트](#3-opcode는-3비트)
4. [명령어 목록](#4-명령어-목록)
5. [1바이트 명령어와 2바이트 명령어](#5-1바이트-명령어와-2바이트-명령어)
6. [PC가 실제로 세는 것](#6-pc가-실제로-세는-것)
7. [명령어를 해석하는 과정](#7-명령어를-해석하는-과정)
8. [마이크로스텝](#8-마이크로스텝)
9. [모든 명령어가 공통으로 하는 일](#9-모든-명령어가-공통으로-하는-일)
10. [각 명령어의 실행 과정](#10-각-명령어의-실행-과정)
11. [제어 신호](#11-제어-신호)
12. [명령어와 마이크로스텝 전체 표](#12-명령어와-마이크로스텝-전체-표)
13. [실제 프로그램 예시](#13-실제-프로그램-예시)
14. [명령어를 만들 때 지켜야 하는 규칙](#14-명령어를-만들-때-지켜야-하는-규칙)

---

# 1. 먼저 알아야 할 용어

## Bit

**Bit(비트)**는 컴퓨터가 다루는 가장 작은 정보 단위다.

```text
0 또는 1
```

---

## Byte

**Byte(바이트)**는 8개의 비트를 묶은 단위다.

```text
1 Byte = 8 Bit
```

우리 컴퓨터는 8비트 단위로 데이터를 다루며 명령어도 기본적으로 8비트씩 RAM에 저장한다.

---

## Address

**Address(주소)**는 RAM의 몇 번째 저장공간을 사용할지 나타내는 번호다.

예를 들어

```text
RAM[130]
```

은 RAM의 130번 주소라는 뜻이다.

우리 컴퓨터는 CDP1824CE RAM을 **5개 모두 사용**하므로 실제 RAM 주소는

```text
0 ~ 159
```

이다.

PC와 MAR 자체는 8비트라 `0~255`를 표현할 수 있지만 현재 `160~255`는 실제 RAM으로 사용하지 않는다.

---

## Instruction

**Instruction(명령어)**은 CPU에게 무엇을 해야 하는지 알려주는 값이다.

예:

```text
LDA 130
```

은

> RAM 130번 주소의 값을 A Register에 가져와라

라는 의미다.

---

## Opcode

**Opcode(Operation Code)**는 명령어에서 **무엇을 할 것인가**를 나타낸다.

예:

```text
001 = LDA
011 = ADD
101 = OUT
```

---

## Operand

**Operand(피연산자)**는 명령어에 필요한 추가 정보다.

우리 컴퓨터에서는 주소를 사용하는 명령어의 Operand가 **8비트 RAM 주소**다.

```text
LDA 130
│   │
│   └── Operand : 주소 130
└────── Opcode  : LDA
```

---

## Register

| 이름 | 뜻 | 역할 |
|---|---|---|
| A | A Register | 계산에 사용할 값 저장 |
| IR | Instruction Register | 현재 명령어 저장 |
| MAR | Memory Address Register | 사용할 RAM 주소 저장 |
| OUT | Output Register | 출력할 값 저장 |

---

# 2. 명령어의 기본 구조

명령어 바이트는 8비트다.

```text
 7 6 5 | 4 3 2 1 0
-------+-----------
Opcode | Reserved
```

현재는 상위 3비트를 Opcode로 사용하고 하위 5비트는 확장용으로 남겨둔다.

```text
[ Opcode 3bit ][ 00000 ]
```

예를 들어 Opcode가 `011`이면

```text
01100000
```

이다.

---

# 3. Opcode는 3비트

```text
000
001
010
011
100
101
110
111
```

3비트이므로 최대 8개의 기본 명령어를 구분한다.

IR에서는

```text
IR7 IR6 IR5 | IR4 IR3 IR2 IR1 IR0
------------+---------------------
   Opcode   |      Reserved
```

로 사용한다.

---

# 4. 명령어 목록

| Opcode | 실제 바이트 | 명령어 | 의미 | 길이 |
|---|---|---|---|---:|
| `000` | `00000000` | `NOP` | 아무 동작도 하지 않음 | 1 Byte |
| `001` | `00100000` | `LDA addr` | RAM 값을 A에 저장 | 2 Byte |
| `010` | `01000000` | `STA addr` | A 값을 RAM에 저장 | 2 Byte |
| `011` | `01100000` | `ADD addr` | RAM 값을 A에 더함 | 2 Byte |
| `100` | `10000000` | `SUB addr` | RAM 값을 A에서 뺌 | 2 Byte |
| `101` | `10100000` | `OUT` | A 값을 출력 | 1 Byte |
| `110` | `11000000` | `JMP addr` | 지정한 주소로 이동 | 2 Byte |
| `111` | `11100000` | `HLT` | 컴퓨터 실행 정지 | 1 Byte |

`addr`은 8비트 Address다. 현재 RAM을 대상으로 할 때 유효한 값은 `0~159`이다.

---

# 5. 1바이트 명령어와 2바이트 명령어

## 1바이트 명령어

추가 주소가 필요하지 않은 명령어다.

```text
NOP
OUT
HLT
```

예:

```text
주소 10 : 10100000   ← OUT
```

---

## 2바이트 명령어

주소가 필요한 명령어다.

```text
LDA
STA
ADD
SUB
JMP
```

예를 들어

```text
LDA 130
```

은 다음처럼 저장한다.

```text
주소 0 : 00100000   ← LDA
주소 1 : 10000010   ← 주소 130
```

두 번째 바이트는 **8비트 주소 전체**를 저장한다.

---

## 왜 주소를 따로 저장할까?

현재 RAM은 160 byte이고 주소 범위는 `0~159`다.

159를 표현하려면 8비트 주소 필드가 필요하다.

```text
159 = 10011111₂
```

Opcode는 3비트이고 주소는 8비트이므로

```text
Opcode 3bit + Address 8bit = 11bit
```

한 바이트에 동시에 넣을 수 없다.

그래서 주소형 명령어는

```text
[명령어 byte]
[주소 byte]
```

두 바이트를 사용한다.

이 구조는 나중에 주소 `160~255`를 Memory-Mapped I/O로 확장하더라도 그대로 사용할 수 있다는 장점이 있다.

---

# 6. PC가 실제로 세는 것

PC는 몇 번째 명령어인지를 세는 것이 아니라 **다음에 읽을 바이트 주소**를 센다.

예:

```text
주소  데이터       의미
-----------------------
0     00100000     LDA
1     00010100     20
2     01100000     ADD
3     00010101     21
4     10100000     OUT
5     11100000     HLT
```

따라서

```text
2바이트 명령어 → PC가 2칸 진행
1바이트 명령어 → PC가 1칸 진행
```

한다.

---

# 7. 명령어를 해석하는 과정

RAM에서 읽은 명령어는 IR에 저장한다.

```text
RAM
 │
 ▼
DATA BUS
 │
 ▼
IR
 │ IR7 IR6 IR5
 ▼
74HC139
Instruction Decoder
 │
 ├── NOP
 ├── LDA
 ├── STA
 ├── ADD
 ├── SUB
 ├── OUT
 ├── JMP
 └── HLT
```

74HC139의 두 개 2→4 Decoder 부분을 조합해 3비트 Opcode 8개를 구분한다.

---

# 8. 마이크로스텝

명령어 하나는 여러 작은 단계로 실행한다.

세 번째 74HC161을 Microstep Counter로 사용하고, 첫 번째 74HC138을 Microstep Decoder로 사용한다.

```text
000 → T0
001 → T1
010 → T2
011 → T3
100 → T4
101 → T5
110 → T6
111 → T7
```

현재 가장 긴 기본 명령어는 T0~T5까지 사용한다.

> 두 번째 74HC138은 명령어 실행 단계가 아니라 **RAM #1~#5 선택용 RAM Bank Decoder**로 사용한다.

---

# 9. 모든 명령어가 공통으로 하는 일

모든 명령어는 먼저 Fetch 과정을 거친다.

## T0 - PC 주소 선택

```text
PC ───────────┐
              ▼
          Address MUX
              │
MAR ──────────┘
              │
              ▼
         ADDRESS BUS
              │
              ▼
             RAM
```

T0에서는 PC를 선택한다.

ADDRESS BUS의 `A7~A5`는 RAM Bank Decoder로 가고 `A4~A0`은 각 CDP1824CE 내부 주소로 간다.

---

## T1 - 명령어 읽기

```text
IR ← RAM[PC]
PC ← PC + 1
```

RAM 데이터가 DATA BUS를 통해 IR에 저장된다.

---

# 10. 각 명령어의 실행 과정

## NOP

```text
T0 : PC 주소 선택
T1 : RAM → IR, PC + 1
T2 : 실행 종료
```

---

## LDA addr

지정한 RAM 주소의 값을 A에 저장한다.

```text
A ← RAM[addr]
```

실행:

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1
T2 : PC 주소 선택
T3 : 다음 주소 바이트를 MAR에 저장, PC + 1
T4 : Address MUX를 MAR로 변경
T5 : RAM[MAR] → A
```

예를 들어 `LDA 130`이면 MAR에는 `10000010`이 저장되고 RAM Bank Decoder는 RAM #5를 선택한다.

---

## STA addr

```text
RAM[addr] ← A
```

실행:

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1
T2 : PC 주소 선택
T3 : 다음 주소 바이트를 MAR에 저장, PC + 1
T4 : Address MUX를 MAR로 변경
T5 : A → DATA BUS → RAM[MAR]
```

---

## ADD addr

```text
A ← A + RAM[addr]
```

실행:

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1
T2 : PC 주소 선택
T3 : 다음 주소 바이트를 MAR에 저장, PC + 1
T4 : Address MUX를 MAR로 변경
T5 : ALU 결과 → A
```

---

## SUB addr

```text
A ← A - RAM[addr]
```

ADD와 같은 흐름이며 T5에서 `SUB` 신호를 활성화한다.

```text
A - B = A + NOT(B) + 1
```

74HC86과 74HC283을 이용해 처리한다.

---

## OUT

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1
T2 : A → OUT Register
```

---

## JMP addr

```text
PC ← addr
```

실행:

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1
T2 : PC 주소 선택
T3 : 다음 주소 바이트를 MAR에 저장, PC + 1
T4 : MAR → PC
```

현재 일반 RAM 프로그램으로 점프할 때는 `0~159`를 사용한다. 이후 `160~255`가 시스템 주소로 정의되면 해당 영역의 JMP 허용 여부도 별도로 결정한다.

---

## HLT

```text
T0 : PC 주소 선택
T1 : 명령어를 IR에 저장, PC + 1
T2 : HALT 상태로 전환
```

---

# 11. 제어 신호

| 제어 신호 | 의미 |
|---|---|
| `RAM_OUT` | 선택된 RAM 값을 DATA BUS에 출력 |
| `RAM_WRITE` | BUS 값을 선택된 RAM에 저장 |
| `A_OUT` | A 값을 BUS에 출력 |
| `A_LOAD` | BUS 값을 A에 저장 |
| `IR_LOAD` | BUS 값을 IR에 저장 |
| `MAR_LOAD` | BUS 값을 MAR에 저장 |
| `OUT_LOAD` | BUS 값을 OUT에 저장 |
| `ALU_OUT` | ALU 결과를 BUS에 출력 |
| `PC_COUNT` | PC를 1 증가 |
| `PC_LOAD` | 새 주소를 PC에 저장 |
| `ADDR_SEL` | PC/MAR 중 ADDRESS BUS에 보낼 값을 선택 |
| `SUB` | ALU를 뺄셈 상태로 변경 |
| `STEP_RESET` | Microstep Counter를 T0으로 되돌림 |
| `HALT` | CPU 실행 정지 |

RAM #1~#5 자체의 선택은 별도 명령 제어신호가 아니라 **현재 ADDRESS BUS의 A7~A5를 74HC138 RAM Bank Decoder가 해석**해 결정한다.

예:

```text
ADDRESS = 10000101₂ = 133
A7~A5 = 100 → RAM #5 선택
A4~A0 = 00101 → RAM #5 내부 주소 5
```

---

# 12. 명령어와 마이크로스텝 전체 표

| 명령어 | T0 | T1 | T2 | T3 | T4 | T5 |
|---|---|---|---|---|---|---|
| `NOP` | PC 주소 | IR 저장 | 종료 | - | - | - |
| `LDA` | PC 주소 | IR 저장 | 주소 byte | MAR 저장 | MAR 주소 | RAM → A |
| `STA` | PC 주소 | IR 저장 | 주소 byte | MAR 저장 | MAR 주소 | A → RAM |
| `ADD` | PC 주소 | IR 저장 | 주소 byte | MAR 저장 | MAR 주소 | ALU → A |
| `SUB` | PC 주소 | IR 저장 | 주소 byte | MAR 저장 | MAR 주소 | ALU → A |
| `OUT` | PC 주소 | IR 저장 | A → OUT | - | - | - |
| `JMP` | PC 주소 | IR 저장 | 주소 byte | MAR 저장 | MAR → PC | - |
| `HLT` | PC 주소 | IR 저장 | 정지 | - | - | - |

가장 긴 명령어가 6단계이므로 T0~T7 구조 안에 들어간다.

---

# 13. 실제 프로그램 예시

숫자 5와 3을 더해서 출력한다.

```text
RAM[130] = 5
RAM[150] = 3
```

프로그램:

```text
LDA 130
ADD 150
OUT
HLT
```

RAM에 저장되는 값:

| 주소 | 2진수 | 16진수 | 의미 |
|---:|---|---|---|
| 0 | `00100000` | `0x20` | LDA |
| 1 | `10000010` | `0x82` | 주소 130 |
| 2 | `01100000` | `0x60` | ADD |
| 3 | `10010110` | `0x96` | 주소 150 |
| 4 | `10100000` | `0xA0` | OUT |
| 5 | `11100000` | `0xE0` | HLT |
| 130 | `00000101` | `0x05` | 숫자 5 |
| 150 | `00000011` | `0x03` | 숫자 3 |

이 예시는 **RAM #5 영역까지 실제 프로그램이 접근할 수 있음**을 보여준다.

```text
130 = RAM #5 내부 주소 2
150 = RAM #5 내부 주소 22
```

실행 결과:

```text
A = 5
A = 5 + 3 = 8
OUT = 8
HLT
```

---

# 14. 명령어를 만들 때 지켜야 하는 규칙

## 1. 일반 RAM 주소는 현재 0~159를 사용한다

```text
0 ~ 159 = 실제 CDP1824CE RAM
```

세부 영역:

```text
0   ~ 31  → RAM #1
32  ~ 63  → RAM #2
64  ~ 95  → RAM #3
96  ~ 127 → RAM #4
128 ~ 159 → RAM #5
```

`160~255`는 현재 실제 RAM이 아니다.

```text
160 ~ 255 → 확장 예약 영역
```

이 영역은 이후 Memory-Mapped I/O, Debug, 센서, System Control 등에 사용할 수 있다.

---

## 2. Opcode 바이트의 하위 5비트는 현재 0으로 둔다

```text
Opcode | Reserved
 3bit  |  5bit
```

예:

```text
LDA = 00100000
```

Reserved 영역은 향후 우리만의 확장 명령어 또는 Addressing Mode에 사용할 수 있다.

---

## 3. 주소가 필요한 명령어 뒤에는 반드시 8비트 주소 바이트가 있어야 한다

잘못된 예:

```text
주소 0 : LDA
주소 1 : ADD
```

올바른 예:

```text
주소 0 : LDA
주소 1 : 130
주소 2 : ADD
주소 3 : 150
```

---

## 4. 프로그램과 데이터가 같은 RAM을 사용한다

하드웨어가 프로그램 영역과 데이터 영역을 강제로 나누지는 않는다.

예를 들어 다음처럼 운영할 수 있다.

```text
0   ~ 63  → 프로그램
64  ~ 159 → 데이터
```

또는 프로그램 크기에 따라 다른 배치를 사용할 수 있다.

---

## 5. DATA BUS 출력은 한 순간에 하나만 허용한다

```text
RAM_OUT = ON
ALU_OUT = ON
```

처럼 서로 다른 장치가 동시에 BUS를 구동하지 않도록 한다.

원칙은

> **BUS 출력 장치는 한 번에 하나**

이다.

---

## 6. RAM Bank 선택은 주소에 의해 자동으로 결정한다

프로그램이 `RAM #4`, `RAM #5`라는 이름을 직접 지정하지 않는다.

그냥 8비트 주소를 사용한다.

예:

```text
LDA 40  → 주소 40  → RAM #2
LDA 100 → 주소 100 → RAM #4
LDA 140 → 주소 140 → RAM #5
```

하드웨어의 74HC138 RAM Bank Decoder가 주소 상위 3비트를 보고 올바른 CDP1824CE를 자동 선택한다.

---

# 최종 요약

```text
명령어 크기     : 기본 1 byte
주소형 명령어   : 2 byte
Opcode          : 3 bit
주소 Operand    : 8 bit
PC / MAR        : 8 bit
실제 RAM         : 160 byte
RAM 주소         : 0~159
확장 예약 주소   : 160~255
RAM Bank Decoder: 74HC138
```

> 따라서 현재 ISA는 **8비트 주소 Operand를 사용하여 5개의 CDP1824CE, 총 160 byte 메모리 전체에 접근할 수 있다.**
