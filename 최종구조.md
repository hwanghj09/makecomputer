# 최종 구조 — MAKECOMPUTER/8

> 이 문서는 `makecomputer` 프로젝트의 **최종 논리 구조 기준**이다.
>
> 목표는 기존 8비트 CPU 본체를 유지하면서, 최소한의 명령어 확장과 주변장치를 추가하여 **전원 ON → Monitor 자동 부팅 → PS/2 키보드 입력 → 20×4 문자 LCD 출력 → RAM 확인/수정 → 사용자 프로그램 실행**이 가능한 독립형 브레드보드 컴퓨터를 만드는 것이다.
>
> 이후 실제 핀 배선도를 작성할 때는 이 문서를 구조 기준으로 사용한다.

---

# 1. 최종 설계 원칙

1. 기존 8비트 CPU의 기본 데이터 경로는 유지한다.
2. PC와 MAR는 기존처럼 8비트다.
3. RAM은 CDP1824CE 5개 = 160 byte를 유지한다.
4. 기존 상위 3bit Opcode 체계를 버리지 않는다.
5. 기존 `000xxxxx` 영역을 확장 명령 그룹으로 사용한다.
6. 조건 분기를 위해 Zero Flag를 추가한다.
7. 키보드와 LCD는 Memory-Mapped I/O로 연결한다.
8. Boot ROM은 CPU를 대신하는 별도 MCU가 아니라 프로그램 저장장치로만 사용한다.
9. RESET 직후에는 Monitor ROM에서 실행한다.
10. 사용자 프로그램을 실행할 때만 Monitor Mode를 끄고 RAM 실행으로 전환한다.
11. Arduino/Raspberry Pi는 완성 시스템의 CPU 또는 키보드/LCD 제어기로 사용하지 않는다.

---

# 2. 전체 구조

```text
                    PS/2 Keyboard
                         │
                  Clock / Data
                         │
                         ▼
              ┌────────────────────┐
              │ Keyboard Interface │
              │ 74HC14             │
              │ 74HC164 ×2         │
              │ 74HC161 Counter    │
              │ 74HC273 Register   │
              │ AT28C64B ASCII ROM │
              │ KEY_READY Flag     │
              └─────────┬──────────┘
                        │
                        ▼
===================== DATA BUS =====================
        │                    │                  │
        ▼                    ▼                  ▼
 ┌─────────────┐      ┌─────────────┐    ┌─────────────┐
 │ 8bit CPU    │      │ Boot ROM    │    │ 20×4 LCD    │
 │ PC / MAR    │      │ AT28C64B    │    │ HD44780     │
 │ A / IR      │      │ Monitor     │    │ Parallel    │
 │ ALU / Z     │      └─────────────┘    └─────────────┘
 └──────┬──────┘
        │
        ▼
  CDP1824CE ×5
   RAM 160 byte
```

---

# 3. 기본 CPU에서 유지하는 구조

```text
NE555 Clock

74HC161 ×2
→ 8bit Program Counter

74HC161 ×1
→ Microstep Counter

74HC273
→ A Register
→ IR
→ MAR
→ OUT Register

74HC283 ×2 + 74HC86 ×2
→ 8bit ADD / SUB ALU

74HC157 ×2
→ PC / MAR Address 선택

74HC157 ×2
→ A / ALU 결과 선택

74HC245
→ RAM ↔ DATA BUS

74HC245
→ A/ALU → DATA BUS

74HC245
→ 기존 수동 입력 → DATA BUS

74HC138
→ Microstep T0~T7

74HC138
→ RAM Bank Decoder

74HC139
→ 기본 Opcode Decoder
```

기존 CPU를 먼저 정상 동작시키고 이후 확장부를 연결한다.

---

# 4. 최종 메모리 맵

## RAM

```text
0   ~  31 : RAM #1
32  ~  63 : RAM #2
64  ~  95 : RAM #3
96  ~ 127 : RAM #4
128 ~ 159 : RAM #5
```

총 RAM:

```text
160 byte
```

## Memory-Mapped I/O

```text
160 : KEY_STATUS
161 : KEY_DATA
162 : LCD_DATA
163 : LCD_COMMAND

164 ~ 191 : 예약
192 ~ 255 : 예약
```

기존 `BOOT_CONTROL = 164` 방식은 최종안에서 사용하지 않는다.

Monitor에서 RAM 프로그램으로 넘어갈 때는 별도의 `RUN addr` 확장 명령으로 **PC 변경과 Monitor Mode 해제를 동시에 수행**한다.

---

# 5. I/O 주소 디코딩

기존 RAM Bank Decoder는 `A7~A5`를 사용한다.

```text
A7 A6 A5
000 → RAM #1
001 → RAM #2
010 → RAM #3
011 → RAM #4
100 → RAM #5
101 → I/O 영역 160~191
110 → 예약
111 → 예약
```

`101`이 선택되었을 때 추가 74HC138을 이용해 `A2~A0`을 디코딩한다.

단, `A4=A3=0`일 때만 I/O Decoder를 Enable한다.

```text
A7~A5 = 101
A4 A3 = 00
          │
          ▼
      74HC138
       A2~A0
          │
          ├─ 000 → 160 KEY_STATUS
          ├─ 001 → 161 KEY_DATA
          ├─ 010 → 162 LCD_DATA
          ├─ 011 → 163 LCD_COMMAND
          └─ 100~111 → 예약
```

---

# 6. 최종 명령어 인코딩 방식

기존 명령어 구조를 유지한다.

```text
IR7 IR6 IR5 | IR4 IR3 IR2 IR1 IR0
------------+---------------------
 기본 Opcode | 확장/예약 영역
```

기존 기본 Opcode:

```text
00100000  LDA addr
01000000  STA addr
01100000  ADD addr
10000000  SUB addr
10100000  OUT
11000000  JMP addr
11100000  HLT
```

`000xxxxx` 그룹만 확장 명령에 사용한다.

최종 V1 확장 명령은 다음과 같이 고정한다.

| 코드 | 명령 | 길이 | 의미 |
|---|---|---:|---|
| `00000000` | `NOP` | 1 | 아무 동작 없음 |
| `00000001` | `LDI value` | 2 | 다음 바이트를 A에 직접 저장 |
| `00000010` | `CMP addr` | 2 | A와 RAM/I/O 값을 빼서 비교하되 A는 변경하지 않음 |
| `00000011` | `JZ addr` | 2 | Z=1이면 PC←addr |
| `00000100` | `JNZ addr` | 2 | Z=0이면 PC←addr |
| `00000101` | `RUN addr` | 2 | PC←addr과 동시에 MONITOR_MODE←0 |
| `00000110` | Reserved | - | 향후 확장 |
| `00000111` | Reserved | - | 향후 확장 |

V1에서는 `IR4=0`, `IR3=0`을 조건으로 하고 `IR2~IR0`으로 위 8개를 구분한다.

확장 명령 디코딩에는 기존 예비 `74HC139` 한 개를 사용한다.

---

# 7. LDI 동작

```text
LDI value
```

은 다음 바이트 자체를 A에 저장한다.

예:

```text
00000001
01000001
```

실행 결과:

```text
A ← 01000001
```

Monitor Firmware에서 LCD 명령값, 문자 상수, 초기화 데이터 등을 만들 때 사용한다.

개념적 마이크로스텝:

```text
T0 : PC 선택
T1 : Instruction → IR, PC+1
T2 : PC 선택
T3 : [PC] → A, PC+1
```

Monitor Mode에서는 `[PC]`가 Boot ROM을 의미하고, RAM Mode에서는 RAM을 의미한다.

---

# 8. CMP와 Zero Flag

`CMP addr`는 실제로는 subtraction을 수행하지만 결과를 A에 저장하지 않는다.

```text
ALU_RESULT = A - MEMORY[addr]
A          = 기존 값 유지
Z          = ALU_RESULT가 0인지 저장
```

예:

```text
A = 'M'
RAM[150] = 'M'

CMP 150
```

결과:

```text
A = 'M' 그대로
Z = 1
```

비교 대상 문자 상수는 Monitor 부팅 시 RAM의 일부 위치에 미리 저장한다.

예:

```text
RAM[150] = 'M'
RAM[151] = 'S'
RAM[152] = 'R'
RAM[153] = 'C'
RAM[154] = 'H'
```

이렇게 하면 기존 RAM→ALU B 입력 경로를 그대로 사용할 수 있어 ALU 배선을 크게 바꾸지 않는다.

---

# 9. Zero Detector

ALU 결과 8비트가 전부 0인지 검사한다.

```text
R7 ─┐
R6 ─┤
R5 ─┤
R4 ─┤
R3 ─┼─ OR Tree ─ NOT ─► ZERO
R2 ─┤
R1 ─┤
R0 ─┘
```

논리식:

```text
ZERO = NOT(R7 OR R6 OR R5 OR R4 OR R3 OR R2 OR R1 OR R0)
```

ZERO 값은 74HC74의 Flip-Flop 한 개에 저장하여 `Z Flag`로 사용한다.

Z Flag는 최소 다음 명령에서 갱신한다.

```text
ADD
SUB
CMP
```

`CMP`에서는 A Register의 Clock을 발생시키지 않고 Z Flag만 저장한다.

---

# 10. JZ / JNZ

## JZ addr

```text
if Z == 1:
    PC ← addr
else:
    계속 실행
```

## JNZ addr

```text
if Z == 0:
    PC ← addr
else:
    계속 실행
```

개념적 실행:

```text
T0 : PC 선택
T1 : Instruction → IR, PC+1
T2 : PC 선택
T3 : 다음 바이트 → MAR, PC+1
T4 : 조건이 참이면 MAR → PC
```

이 두 명령으로 Monitor의 입력 대기, 문자 구분, 반복문을 구현한다.

---

# 11. Monitor Mode와 Boot ROM

## 핵심 원칙

단순히 주소 `0~159` 전체를 ROM/RAM으로 바꾸는 Overlay는 사용하지 않는다.

대신 **현재 주소를 PC가 내고 있는지 MAR가 내고 있는지**를 이용한다.

RESET 직후:

```text
MONITOR_MODE = 1
PC = 0
```

Monitor Mode에서:

```text
주소 출처 = PC
→ Boot ROM에서 읽음

주소 출처 = MAR
→ RAM 또는 Memory-Mapped I/O에 접근
```

즉 Monitor Firmware는 ROM에서 실행되지만 데이터는 정상 RAM과 Keyboard/LCD를 사용할 수 있다.

```text
                    Address Source

PC ───────────────► Boot ROM
                     ▲
                     │ MONITOR_MODE = 1

MAR ──────────────► RAM / I/O
```

이 방식을 **Fetch-only Monitor ROM 구조**로 사용한다.

---

# 12. Boot ROM Bus 제어

Monitor Mode에서 PC가 주소를 선택한 읽기 사이클일 때만 Boot ROM이 DATA BUS를 구동한다.

개념 조건:

```text
ROM_FETCH = MONITOR_MODE
            AND PC_SELECTED
            AND READ_CYCLE
```

`ROM_FETCH=1`이면:

```text
Boot ROM → DATA BUS
RAM 출력 금지
Keyboard 출력 금지
```

`ROM_FETCH=0`이면 Boot ROM은 High-Z 상태가 되고 기존 RAM/I/O가 정상적으로 DATA BUS를 사용할 수 있다.

Boot ROM 출력은 예비 74HC245 하나를 통해 DATA BUS에 연결한다.

---

# 13. Boot ROM 주소

CPU의 PC는 8비트이므로 V1 Monitor Firmware는 Boot ROM의 한 페이지만 사용한다.

```text
EEPROM A0~A7  ← PC0~PC7
EEPROM A8~A12 → 0으로 고정
```

따라서 V1에서 직접 실행 가능한 Monitor 공간은:

```text
256 byte
```

이다.

AT28C64B 자체 용량은 더 크지만 V1에서는 첫 256 byte만 사용한다.

이 제약 때문에 첫 Monitor는 최대한 작게 작성한다.

초기 명령은 긴 Shell보다 다음과 같은 Compact Monitor 형태를 우선한다.

```text
H            도움말
M aa         RAM aa 읽기
S aa vv      RAM aa에 vv 저장
R aa         RAM aa부터 실행
C            화면 지우기
```

추후 필요하면 ROM Bank 확장을 별도 기능으로 추가할 수 있으나 V1 최종 구조에는 포함하지 않는다.

---

# 14. RUN 명령과 RAM 프로그램 실행

Monitor Mode를 단순 I/O write로 즉시 끄면 현재 ROM PC 주소에서 RAM으로 갑자기 바뀌어 잘못된 명령을 실행할 수 있다.

따라서 최종안에서는 `RUN addr` 명령을 사용한다.

```text
RUN addr
```

마지막 실행 단계에서 **동시에**:

```text
PC ← addr
MONITOR_MODE ← 0
```

를 수행한다.

따라서 다음 Fetch부터:

```text
RAM[addr]
```

의 사용자 프로그램이 정상적으로 실행된다.

사용자 프로그램에서 Monitor로 돌아올 때는 V1에서는 `RESET` 버튼을 사용한다.

```text
RESET
 ↓
MONITOR_MODE = 1
PC = 0
 ↓
Monitor 다시 시작
```

---

# 15. PS/2 Keyboard Interface

최종 입력 경로:

```text
PS/2 Keyboard
      │
      ├─ CLOCK ─► 74HC14
      │
      └─ DATA ──► 74HC164 ×2
                      │
                      ├─► 74HC161 Bit Counter
                      │
                      ▼
                   74HC273
                Scan Code Register
                      │
                      ▼
                  AT28C64B #1
                Scan Code → ASCII
                      │
                      ▼
                    KEY_DATA
```

PS/2의 `F0` 같은 특수 코드는 ASCII Translation ROM에서 일반 문자와 겹치지 않는 특수값으로 변환하고 Monitor 소프트웨어가 무시/처리하도록 한다.

V1에서는 Shift 상태를 복잡하게 구현하지 않고 영문 키를 기본적으로 대문자 ASCII로 변환해도 된다.

---

# 16. KEY_STATUS / KEY_DATA

## KEY_STATUS — 주소 160

```text
bit0 = KEY_READY
bit7~1 = 0
```

```text
KEY_READY = 1
→ 읽을 문자가 있음

KEY_READY = 0
→ 새 문자가 없음
```

## KEY_DATA — 주소 161

읽으면 ASCII 8비트를 DATA BUS에 출력한다.

```text
LDA 161
→ A에 문자 저장
```

`KEY_DATA`를 CPU가 정상적으로 읽은 뒤 `KEY_READY`를 0으로 Clear한다.

KEY_READY는 74HC74의 Flip-Flop 하나를 사용한다.

KEY_STATUS와 KEY_DATA 선택에는 기존 예비 74HC157 ×2를 사용하고, 선택된 8비트를 예비 74HC245를 통해 DATA BUS에 연결한다.

---

# 17. LCD Interface

LCD는 **20×4 HD44780 호환 5V 병렬형**을 사용한다.

```text
CPU DATA BUS D0~D7
        │
        └────────► LCD D0~D7

LCD RW → GND
```

CPU에서는 LCD를 읽지 않고 쓰기 전용으로 사용한다.

```text
162 LCD_DATA
→ RS = 1
→ 문자 출력

163 LCD_COMMAND
→ RS = 0
→ LCD 명령 전달
```

해당 주소에 `STA`가 실행될 때 LCD `E` 신호를 한 번 발생시켜 데이터를 확정한다.

LCD Busy Flag를 읽지 않고 Firmware에서 충분한 대기 시간을 두는 방식을 사용한다.

---

# 18. 추가 Flip-Flop 배정

새로 구매하는 `74HC74 ×2`에는 총 4개의 D Flip-Flop이 있다.

최종 V1 배정:

```text
74HC74 #1A → Z Flag
74HC74 #1B → MONITOR_MODE

74HC74 #2A → KEY_READY
74HC74 #2B → 예비
```

PS/2 Break/Shift 같은 상태는 V1에서는 가능한 한 Monitor 소프트웨어와 ASCII Translation ROM으로 처리하여 하드웨어 상태 비트를 늘리지 않는다.

---

# 19. 기존 예비 IC 활용

기본 CPU 완성 후 예상되는 예비 IC를 다음처럼 사용한다.

```text
74HC161 예비 2개
→ 1개 PS/2 Bit Counter
→ 1개 예비

74HC273 예비 1개
→ PS/2 Scan Code Register

74HC245 예비 2개
→ Boot ROM → DATA BUS
→ Keyboard I/O → DATA BUS

74HC157 예비 2개
→ KEY_STATUS / KEY_DATA 8bit 선택

74HC138 예비 1개
→ I/O Address Decoder

74HC139 예비 2개
→ 1개 Extended Instruction Decoder
→ 1개 예비
```

Zero Detector와 새 Control Logic은 기존 예비 74HC32 / 74HC04 / 74HC08을 우선 사용한다.

단, 최종 핀 단위 Control Logic을 최적화한 뒤 게이트 수가 부족하면 `74HC08`, `74HC32`, `74HC04`를 각각 1개 정도 추가 구매할 수 있다. 이 부분은 **핀 배선도 확정 단계에서 최종 수량을 다시 계산한다.**

---

# 20. Monitor Firmware V1

전원 ON:

```text
RESET
 ↓
MONITOR_MODE = 1
PC = 0
 ↓
Boot ROM Fetch
```

Firmware 순서:

```text
1. LCD 초기화
2. 화면에 MAKECOMPUTER/8 표시
3. 비교용 ASCII 상수를 RAM 상단에 저장
4. KEY_STATUS Polling
5. KEY_DATA 읽기
6. 입력 문자 LCD Echo
7. 명령 문자 비교
8. Memory Read / Write / Run 수행
9. 다시 입력 대기
```

예상 화면:

```text
MAKECOMPUTER/8
READY
> M 20
05
```

초기 명령:

```text
H       HELP
M aa    MEMORY READ
S aa vv MEMORY WRITE
R aa    RUN
C       CLEAR LCD
```

---

# 21. 최종 데이터 흐름

```text
                         ┌──────────────┐
                         │ PS/2 Keyboard│
                         └──────┬───────┘
                                │
                                ▼
                        Keyboard Receiver
                                │
                          ASCII Translation
                                │
                                ▼
                             KEY_DATA
                                │
                                ▼
=========================== DATA BUS ===========================
        │                       │                       │
        ▼                       ▼                       ▼
     CPU Core                Boot ROM               Text LCD
        │                 (Monitor Mode)            20×4
        │
        ▼
   Address MUX
   PC / MAR Select
        │
        ├── PC + MONITOR_MODE=1 ──► Boot ROM Fetch
        │
        └── MAR ──────────────────► RAM / I/O
                                      │
                     ┌────────────────┼────────────────┐
                     ▼                ▼                ▼
                  RAM 0~159       Keyboard         LCD
```

---

# 22. 최종 성공 조건

1. 기존 8비트 CPU 기본 명령이 정상 실행된다.
2. RAM 160 byte를 정상 읽기/쓰기 한다.
3. `LDI`, `CMP`, `JZ`, `JNZ`, `RUN`이 동작한다.
4. Z Flag가 정상적으로 저장된다.
5. RESET하면 `MONITOR_MODE=1`, `PC=0`이 된다.
6. Monitor Mode의 PC Fetch는 Boot ROM에서 이루어진다.
7. Monitor Mode에서도 MAR를 이용해 RAM과 I/O에 접근할 수 있다.
8. PS/2 Keyboard Scan Code를 외부 MCU 없이 받는다.
9. Scan Code를 ASCII로 변환한다.
10. KEY_READY / KEY_DATA가 정상 동작한다.
11. CPU가 20×4 LCD에 문자와 명령을 출력한다.
12. Monitor에서 RAM 읽기/쓰기 기능을 사용할 수 있다.
13. `RUN addr`를 실행하면 RAM의 사용자 프로그램으로 전환된다.
14. RESET하면 언제든 Monitor로 복귀한다.
15. 완성된 시스템은 Arduino/Raspberry Pi 없이 독립 동작한다.

---

# 최종 한 줄 구조

> **기존 8비트 CPU + 160 byte RAM을 유지하고, `000xxxxx` 확장 명령 그룹, Zero Flag, Fetch-only Monitor ROM, PS/2 Keyboard Interface, Memory-Mapped I/O, 20×4 Text LCD를 추가하여 전원을 켜면 스스로 Monitor로 부팅하고 키보드로 RAM을 조작하거나 프로그램을 실행할 수 있는 독립형 8비트 브레드보드 컴퓨터를 만든다.**
